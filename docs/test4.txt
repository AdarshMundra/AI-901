Question 1
Incorrect
Your Python app successfully generated an image by using a GPT-image-1 deployment.



Which field contains the image data that you decode?

A. url

Explanation
The 'url' field typically contains the URL or path to the location of the image, not the actual image data itself. It is used to reference the location of the image rather than the image data for decoding.
Your answer is incorrect
B. prompt

Explanation
The 'prompt' field is used to provide input or instructions for the GPT-image-1 deployment to generate the image. It does not contain the actual image data that needs to be decoded.
Correct answer
C. b64_json

Explanation
The 'b64_json' field is likely to contain the image data in base64-encoded format, which can be decoded to retrieve the actual image generated by the GPT-image-1 deployment. This field is the most suitable choice for decoding the image data.
D. deployment_name

Explanation
The 'deployment_name' field typically contains the name or identifier of the deployment used in the Python app and is not related to the image data that needs to be decoded. It is not the correct field for retrieving the image data.
Overall explanation
b64_json is correct because Microsoft shows that a successful image generation response includes a data array and that the b64_json field contains the base64-encoded output image data. The same documentation shows this field being decoded in code examples.



This is a practical implementation detail for lightweight apps built on top of image-generation models. The model creates the image, and your app reads the base64 payload from b64_json, decodes it, and saves or processes the resulting bytes.



Why the other options are incorrect:

• A. url is plausible because earlier image-generation patterns and other APIs sometimes return downloadable URLs. However, Microsoft states that GPT-image-1 series models always return base64-encoded images, and the documented field for that returned payload is b64_json. So url is not the right field in this scenario.



• B. prompt is plausible because prompts are central to image generation and are part of the request. But the prompt is input sent to the model, not the output image payload returned by the service. The returned image bytes are represented in the b64_json field.



• D. deployment_name is plausible because deployment identifiers are required when calling Azure-hosted models. However, a deployment name tells the service which deployment to use; it does not hold the generated image data. The response field used for decoding is b64_json.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 2
Correct
You want the generated image to use a transparent background. Complete the request with the correct parameter.

Snippet

result = client.images.generate(
    model="gpt-image-1",
    prompt="A flat icon of a blue rocket",
    n=1,
    size="1024x1024",
    background="transparent",
    ________="png"
)
Which code should replace the missing section?

Your answer is correct
A. output_format

Explanation
The correct parameter to specify the output format of the generated image is "output_format". In this case, setting the output format to "png" will ensure that the image has a transparent background as requested in the prompt.
B. response_format

Explanation
The parameter "response_format" is not a valid parameter for specifying the output format of the generated image. It does not serve the purpose of setting the background to transparent or defining the image format.
C. content_type

Explanation
The parameter "content_type" is not used to specify the output format of the generated image. It is typically used to define the type of content being sent in an HTTP request or response, and it is not relevant to setting the background or image format.
D. image_format

Explanation
The parameter "image_format" is not the correct parameter to specify the output format of the generated image. It does not specifically address the requirement for a transparent background or define the image format in this context.
Overall explanation
output_format is correct because Microsoft says the output_format parameter specifies the format of the generated image, and supported values include PNG and JPEG for GPT-image-1 series models. Microsoft also states that to get a transparent background, you set background to transparent and output_format to PNG.



This is a parameter-selection question rather than a model-choice question. The requirement is not just to generate an image, but to generate one with transparency, and Microsoft ties that behavior to the combination of background="transparent" and PNG output.



Why the other options are incorrect:

• B. response_format is a strong distractor because it sounds similar to output_format. However, Microsoft states that response_format is supported for DALL-E 3 and not for GPT-image-1 series models, which always return base64-encoded images. So it is not the correct parameter for this request.



• C. content_type is plausible because HTTP requests often include content type information. But Microsoft’s image-generation parameters use output_format to control whether the output is PNG or JPEG. content_type is not the documented body parameter for choosing image output format here.



• D. image_format is plausible because many APIs use similar names. However, the Microsoft documentation for GPT-image-1 uses the exact parameter name output_format, not image_format. In code-completion questions, the precise documented field name matters.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 3
Correct
For each of the following statements, determine whether the statement is correct.



Statement 1: Screen-reader-friendly output can support inclusiveness.



Statement 2: Requiring mouse input for every action improves inclusiveness.



Statement 3: Offering both voice and text interaction can support inclusiveness.



Select the correct combination.

A. Yes / Yes / No

Your answer is correct
B. Yes / No / Yes

C. No / Yes / Yes

D. No / No / Yes

Overall explanation
Statement 1 is correct because accessible output helps more people use the system effectively. Inclusive AI design considers different abilities and seeks to remove participation barriers rather than assuming a single ideal user.



Statement 2 is incorrect because forcing one input method can exclude users who cannot comfortably use that method. Statement 3 is correct because multiple interaction methods increase the chance that users with different needs and preferences can engage successfully, which is a core inclusiveness idea.



Why the other options are incorrect:

• A. This option marks Statement 2 as Yes, which is the key problem. Requiring mouse input for every action reduces flexibility and can create accessibility barriers. That is inconsistent with inclusive design, even though Statement 1 is correctly marked Yes.

• C. This option incorrectly marks Statement 1 as No and Statement 2 as Yes. Screen-reader-friendly output is a strong example of designing for a wider range of users, while forced mouse input is exclusionary. Because two statement judgments are wrong, the combination cannot be correct.

• D. This option correctly marks Statement 2 as No, but it incorrectly marks Statement 1 as No. Accessible output and multiple interaction methods both support inclusiveness. Missing Statement 1 makes the full combination incorrect.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 4
Correct
A team is designing an AI help desk for a global workforce. Which TWO actions best support inclusiveness? (Select TWO.)

Your selection is correct
A. Provide captions and voice input

B. Limit the app to one interaction style

Your selection is correct
C. Test with users who have different abilities

D. Store every transcript for seven years

E. Assign one executive approver

Overall explanation
Providing captions and voice input expands how people can engage with the solution. Testing with users who have different abilities also supports inclusiveness because it helps identify barriers that a narrow design team might miss.



This question also checks whether you can separate design-for-use considerations from governance and retention choices. Inclusiveness is about making participation practical for more people, not about making the system easier to audit or keeping data longer than needed.



Why the other options are incorrect:

• B. Limiting the app to one interaction style moves in the opposite direction of inclusiveness. It assumes all users can and want to interact in the same way. Inclusive design typically provides flexibility rather than a single path.

• D. Transcript retention is mainly a data handling and governance decision. Depending on the context, it may raise privacy or compliance questions rather than improve inclusiveness. Keeping data longer does not help more users access or use the AI system effectively.

• E. Assigning an executive approver relates to accountability and oversight. That can be useful for governance, but it is not the strongest action for making the experience accessible to a broader range of users. Inclusive design must focus on user participation and accessibility.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 5
Correct
Which THREE pairs are correctly matched? (Select THREE.)

Your selection is correct
A. Inclusiveness — supports users with different abilities

B. Privacy and security — explains model limitations

Your selection is correct
C. Fairness — avoids disadvantaging groups

D. Accountability — encrypts data at rest

Your selection is correct
E. Transparency — helps users understand system behavior

F. Reliability and safety — provides captions and voice input

Overall explanation
Inclusiveness is about empowering more people to use AI systems effectively, including people with different abilities and access needs. Fairness is about avoiding unfair impact across groups, and transparency is about helping people understand the system’s behavior, intended use, and limitations.



This format tests whether you can distinguish closely related responsible AI principles. AI-901 often separates these principles through short scenario language, so knowing which phrase maps to which principle is important.



Why the other options are incorrect:

• B. Explaining model limitations is a transparency activity, not a privacy and security activity. Privacy and security are about protecting data, access, and system integrity. The pair sounds plausible because both are trust-related, but they are not the same principle.

• D. Encrypting data at rest is a privacy and security control. Accountability focuses on who is responsible for decisions, oversight, and governance around AI use. The pair is incorrect because it swaps a technical protection control into the wrong principle.

• F. Providing captions and voice input is a strong inclusiveness example because it supports different user needs and interaction styles. Reliability and safety are more about dependable operation, expected behavior, and resisting harmful or unsafe outcomes. The pair is plausible because both matter for user trust, but it is still mismatched.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 6
Correct
An AI kiosk will be used in a noisy factory and in a quiet office. Which change most directly improves inclusiveness?

Your answer is correct
A. Add text and speech interaction

Explanation
Adding text and speech interaction to the AI kiosk allows users in both noisy factory and quiet office environments to interact with the kiosk effectively. This change improves inclusiveness by providing multiple modes of interaction that cater to different user preferences and environmental conditions.
B. Increase log retention

Explanation
Increasing log retention does not directly improve inclusiveness for users in noisy factory and quiet office environments. While log retention may be important for tracking system activities, it does not address the issue of accommodating users in different environmental conditions.
C. Publish the approval owner

Explanation
Publishing the approval owner is not directly related to improving inclusiveness for users in noisy factory and quiet office environments. This action may be relevant for organizational transparency, but it does not address the need to make the AI kiosk more accessible and user-friendly in diverse settings.
D. Require sign-in every hour

Explanation
Requiring sign-in every hour does not directly enhance inclusiveness for users in noisy factory and quiet office environments. This measure may be related to security protocols but does not specifically address the challenge of ensuring that all users, regardless of their environment, can effectively interact with the AI kiosk.
Overall explanation
Adding both text and speech interaction supports more user contexts and more user needs. In a noisy factory, text may be easier than audio, while in other situations speech may help users who cannot conveniently type, so the design becomes more inclusive.



The key exam skill here is identifying the option that broadens access instead of the option that strengthens governance or security. Inclusiveness is about enabling effective participation for a wider range of people and environments.



Why the other options are incorrect:

• B. Increasing log retention may support auditing or compliance in some cases, but it does not make the kiosk easier for more people to use. It can also introduce separate privacy considerations. The question asks for the change that most directly improves inclusiveness, which this does not do.

• C. Publishing the approval owner relates to accountability because it clarifies who is responsible for oversight or escalation. That is useful governance information, but it does not improve accessibility or flexibility of interaction. Users still face the same usability barriers.

• D. Requiring sign-in every hour is primarily an access-control choice and can even reduce usability for some users. It is closer to security policy than inclusiveness. More frequent sign-ins do not broaden who can successfully interact with the AI solution.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 7
Correct
A team reviews an AI résumé assistant before release. Which TWO findings are the strongest inclusiveness concerns? (Select TWO.)

Your selection is correct
A. The interface does not work with screen readers

B. The training data has fewer examples from one age group

Your selection is correct
C. Spoken guidance has no captions

D. The service owner is not documented

E. Customer data is not encrypted

Overall explanation
An interface that does not work with screen readers and spoken guidance without captions both create direct accessibility barriers. Those are strong inclusiveness concerns because they reduce who can use the solution effectively.



The other options are important, but they align to different responsible AI principles. AI-901 often tests this exact distinction by mixing inclusiveness issues with fairness, accountability, and privacy and security in the same answer set.



Why the other options are incorrect:

• B. Fewer examples from one age group points more strongly to fairness and representativeness concerns. That issue can lead to uneven model behavior across groups, which is important, but it is not primarily an accessibility or interaction-path issue. The question asks specifically for the strongest inclusiveness concerns.

• D. An undocumented service owner is an accountability problem because it weakens ownership and governance. It may make issue escalation harder, but it does not directly prevent users from accessing or using the résumé assistant. That is why it is not one of the two best answers here.

• E. Lack of encryption is a privacy and security concern. It matters for protecting personal data, but it does not directly determine whether people with different needs can use the system. The question is about inclusive access, not data protection.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 8
Correct
For each of the following statements, determine whether the statement is correct.



Statement 1: delete_analyzer is the SDK method used to submit content for analysis.



Statement 2: A custom analyzer for images should use base_analyzer_id="prebuilt-video".



Statement 3: Content Understanding analyzers can return structured outputs such as markdown, JSON fields, and segments.



Select the correct combination.

A. Yes / Yes / No

B. Yes / No / Yes

C. No / Yes / Yes

Your answer is correct
D. No / No / Yes

Overall explanation
Statement 1 is incorrect. The SDK examples use begin_analyze to submit content for analysis, while delete_analyzer is used for cleanup and removal of an analyzer resource.



Statement 2 is also incorrect because image custom analyzers inherit from prebuilt-image, not prebuilt-video. Statement 3 is correct because the analyzer reference explains that analyzers define structured output such as markdown, JSON fields, and segments, and the quickstart examples show returned markdown and fields in the results.



Why the other options are incorrect:

• A. Yes / Yes / No. This option incorrectly treats both method usage and base-analyzer selection as true. The docs show begin_analyze for submission and prebuilt-image for image inheritance, so the first two positions should both be No. It also incorrectly rejects the documented structured outputs that analyzers can return.

• B. Yes / No / Yes. This option gets Statement 2 and Statement 3 right in spirit, but it still incorrectly marks Statement 1 as Yes. That error matters because delete_analyzer is not the analysis-submission method. The combination therefore remains incorrect even though it correctly recognizes the structured-output behavior.

• C. No / Yes / Yes. This option correctly rejects Statement 1 and accepts Statement 3, but it incorrectly marks Statement 2 as Yes. For image scenarios, the documented custom-analyzer pattern uses prebuilt-image, not prebuilt-video. Because the second statement is false, this combination cannot be the right answer.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 9
Correct
Which question is most directly tied to inclusiveness when evaluating an AI solution?

A. Is personal data encrypted during storage and transit?

Explanation
Ensuring personal data encryption during storage and transit is important for data privacy and security but is not directly tied to inclusiveness in an AI solution.
B. Can users understand the model’s limitations?

Explanation
Understanding the model's limitations is crucial for transparency and trust in AI systems, but it does not directly address inclusiveness in terms of accessibility for users with different abilities.
Your answer is correct
C. Can people with different abilities use the system effectively?

Explanation
Evaluating whether people with different abilities can effectively use the system is directly tied to inclusiveness in an AI solution. It focuses on accessibility and ensuring that the system can be used by a diverse range of users.
D. Who approves production deployment?

Explanation
The approval of production deployment is related to governance and decision-making processes within an organization, but it is not directly linked to inclusiveness in evaluating an AI solution.
Overall explanation
The best inclusiveness question asks whether people with different abilities can use the system effectively. That directly reflects the responsible AI idea that systems should empower more people and avoid unnecessary participation barriers.



The other questions belong to other principles: data encryption maps to privacy and security, model limitation understanding maps to transparency, and deployment approval maps to accountability or governance. This kind of principle-matching is a core AI-901 skill.



Why the other options are incorrect:

• A. Encryption is a data protection control, so it maps to privacy and security rather than inclusiveness. It is absolutely important in AI systems, but it does not tell you whether the experience is usable by people with different abilities. A secure system can still be inaccessible.

• B. Understanding model limitations is a transparency concern. Users should know what the model does well, where it may fail, and how to use it appropriately. That is different from ensuring the system can be accessed and used by a broad range of people.

• D. Approval of production deployment is about accountability, oversight, and governance. It clarifies decision-making responsibility, but it does not answer whether the solution includes accessible interaction paths. The question asks for the evaluation lens most directly tied to inclusiveness.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 10
Correct
For each of the following statements, determine whether the statement is correct.



Statement 1: Requiring one interaction method for all users supports inclusiveness.



Statement 2: Providing captions for spoken output can support inclusiveness.



Statement 3: Testing with users who have different abilities can support inclusiveness.



Select the correct combination.

A. Yes / Yes / No

B. Yes / No / Yes

Your answer is correct
C. No / Yes / Yes

D. No / No / Yes

Overall explanation
Statement 1 is incorrect because requiring one interaction method assumes all users can access the system in the same way. Inclusive design instead aims to broaden participation through flexible input and output choices.



Statement 2 is correct because captions provide an accessible alternative to audio output. Statement 3 is also correct because testing with users who have different abilities helps teams find barriers early and design experiences that work for a wider range of people.



Why the other options are incorrect:

• A. This option incorrectly marks Statement 1 as Yes, which reverses the main inclusiveness idea in the question. A single required interaction style can exclude people rather than empower them. It also incorrectly marks Statement 3 as No, even though diverse user testing supports inclusive design.

• B. This option gets Statement 3 correct, but it incorrectly marks Statement 1 as Yes and Statement 2 as No. Captions are a strong example of an inclusive design choice because they broaden access to spoken output. With two statement judgments wrong, this combination cannot be correct.

• D. This option correctly marks Statement 1 as No, but it incorrectly marks Statement 2 as No. Providing captions is one of the clearest examples of supporting users with different needs. Because Statement 2 should be Yes, the overall combination is incorrect.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 11
Correct
You deployed an image generation model and want to test prompt changes visually in Foundry before writing code.



Which Foundry experience should you use?

A. Azure AI Search

Explanation
Azure AI Search is a service that allows you to build a search experience over your data. It is not specifically designed for testing prompt changes visually in Foundry before writing code, so it is not the correct choice for this scenario.
Your answer is correct
B. Images playground

Explanation
The Images playground in Foundry is specifically designed for testing prompt changes visually. It allows you to interact with the image generation model and see the results of different prompts without writing any code, making it the correct choice for this scenario.
C. Speech playground

Explanation
The Speech playground in Foundry is designed for testing speech-related functionalities, not for testing prompt changes visually in image generation models. Therefore, it is not the correct choice for this scenario.
D. OCR workflow

Explanation
The OCR workflow in Foundry is used for Optical Character Recognition tasks, not for testing prompt changes visually in image generation models. It is not the correct choice for this scenario.
Overall explanation
Images playground is correct because Microsoft Foundry routes image-generation models such as gpt-image-1 to the images playground. Microsoft also describes it as the place to experiment with image-generation models visually before moving into production code.



That matches the scenario exactly: the user wants to try prompts, inspect visual results, and validate behavior first. The playground is designed for quick experimentation, parameter tuning, and later code handoff through the built-in code-view workflow.



Why the other options are incorrect:

• A. Azure AI Search is a plausible distractor because it often appears in generative AI solutions. However, it is a retrieval and indexing service, not the Foundry experience for generating new images from prompts. The Microsoft guidance for image generation points to the images playground, not search, as the no-code testing surface.



• C. Speech playground is plausible because Foundry has multiple playground experiences. But speech tooling is for audio and speech scenarios, while the question is about creating visual outputs from an image-generation model. Microsoft explicitly says image-generation models are presented in the images playground.



• D. OCR workflow is plausible because OCR is image-related and learners often group all vision features together. However, OCR extracts text from existing images and documents, while this question is about generating new visuals from prompts. The correct Foundry experience for that workflow is the images playground.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 12
Correct
You need the preferred deployment option in Foundry that offers the widest range of capabilities, including regional, data zone, or global processing, and supports standard and provisioned throughput choices.



Which deployment option should you select?

A. Serverless API endpoint

Explanation
A Serverless API endpoint in Foundry is a deployment option that provides on-demand scaling and automatic resource allocation based on usage. While it offers flexibility and cost-effectiveness, it may not provide the widest range of capabilities, including regional, data zone, or global processing, and support for standard and provisioned throughput choices.
B. Managed compute

Explanation
Managed compute in Foundry is a deployment option that involves using managed virtual machines or containers to run applications. While it offers control over the computing resources and scalability, it may not provide the widest range of capabilities, including regional, data zone, or global processing, and support for standard and provisioned throughput choices.
Your answer is correct
C. Standard deployment in Foundry resources

Explanation
Standard deployment in Foundry resources is the correct choice as it offers the widest range of capabilities, including regional, data zone, or global processing, and supports standard and provisioned throughput choices. This deployment option provides flexibility, scalability, and performance optimization for various AI workloads.
D. Azure AI Search

Explanation
Azure AI Search is a service provided by Microsoft Azure for building search solutions. While it offers powerful search capabilities, it is not a deployment option in Foundry that provides the specific capabilities mentioned in the question, such as regional, data zone, or global processing, and support for standard and provisioned throughput choices.
Overall explanation
Standard deployment in Foundry resources is the best match because Microsoft describes it as the preferred deployment option and says it offers the widest range of capabilities. The documentation also states that this option supports regional, data zone, or global processing and includes standard and provisioned throughput options.



Serverless API endpoints and managed compute both have valid use cases, but they do not provide the same breadth of deployment capability described in the question. Serverless API deployments are regional, while managed compute is used for dedicated compute scenarios and is not the preferred broad-capability choice when standard deployment in Foundry resources is available.



Why the other options are incorrect:

• A. Serverless API endpoint is plausible because it is a real Foundry deployment option and supports API-based access. However, Microsoft says serverless API deployments are regional and available only in AI Hub resources. That does not match the requirement for the widest capability range across regional, data zone, and global processing, nor does it match the preferred-option wording.



• B. Managed compute is plausible because it is also a real Foundry deployment choice and provides dedicated infrastructure. However, Microsoft positions it for scenarios that need dedicated compute and notes that it requires compute quota and is billed per compute uptime. It is not the preferred broad-capability option described in the stem.



• D. Azure AI Search is a plausible distractor because learners often confuse AI services with model deployment choices. But Azure AI Search is not one of the deployment options listed in the Foundry deployment overview. The question is about how a model is deployed, not about a retrieval or indexing service.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 13
Correct
You need to deploy a Hugging Face model and host it on dedicated compute in an AI project. Your subscription already has compute quota.



Which deployment option should you choose?

A. Standard deployment in Foundry resources

Explanation
Standard deployment in Foundry resources may not be the most suitable option for hosting a Hugging Face model on dedicated compute. Foundry resources are typically used for data management and collaboration, rather than hosting AI models on dedicated compute.
Your answer is correct
B. Managed compute

Explanation
Managed compute is the correct choice for deploying a Hugging Face model and hosting it on dedicated compute in an AI project. Managed compute provides dedicated resources for running AI workloads, ensuring optimal performance and scalability for hosting the model.
C. Serverless API endpoint

Explanation
Serverless API endpoint is not the ideal option for hosting a Hugging Face model on dedicated compute. Serverless endpoints are more suitable for lightweight and event-driven workloads, rather than hosting AI models that require dedicated compute resources.
D. Foundry Playground

Explanation
Foundry Playground is not the appropriate choice for deploying a Hugging Face model and hosting it on dedicated compute. Foundry Playground is typically used for experimentation and prototyping, rather than hosting production-grade AI models on dedicated compute.
Overall explanation
Managed compute is correct because Microsoft states that managed compute lets you host a model on dedicated compute, requires compute quota, and is the required deployment option for certain model collections, including Hugging Face. That matches the scenario directly.



This question is testing deployment-option identification, not model prompting or testing. The key clues are “Hugging Face,” “dedicated compute,” and “compute quota,” all of which point to managed compute rather than a serverless or standard deployment path.



Why the other options are incorrect:

• A. Standard deployment in Foundry resources is tempting because it is the preferred deployment option whenever possible. However, the documentation specifically calls out managed compute as the required path for certain collections such as Hugging Face. That makes standard deployment the wrong choice for this particular requirement.



• C. Serverless API endpoint is plausible because it is another way to deploy models through an API. But Microsoft describes serverless API endpoints as pay-as-you-go regional deployments, not dedicated compute deployments. The stem explicitly asks for dedicated compute and mentions quota, which align with managed compute instead.



• D. Foundry Playground is plausible because learners often associate it with model use in Foundry. However, a playground is for interaction and testing, not for selecting a deployment hosting type. The question is about how the model is hosted, and Playground is not one of the listed deployment options.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 14
Correct
You want a chat model to produce more focused and deterministic answers. Lowering ________ is the most direct configuration choice.



Which answer best completes the sentence?

Your answer is correct
A. temperature

Explanation
Lowering the temperature parameter in a chat model configuration reduces the randomness in the generated responses, making the answers more focused and deterministic. This is the most direct choice to achieve the desired outcome of producing more focused answers in the chat model.
B. top_p

Explanation
The top_p parameter in a chat model configuration controls the nucleus sampling, which determines the probability of sampling from the most likely tokens. While adjusting this parameter can influence the diversity of responses, it is not the most direct choice for producing more focused and deterministic answers.
C. presence_penalty

Explanation
The presence_penalty parameter in a chat model configuration penalizes the presence of certain tokens in the generated responses. While it can help in shaping the responses, it may not be the most direct choice for achieving more focused and deterministic answers in the chat model.
D. frequency_penalty

Explanation
The frequency_penalty parameter in a chat model configuration penalizes the frequency of certain tokens in the generated responses. While it can impact the repetition of certain words, it may not be the most direct choice for making the answers more focused and deterministic in the chat model.
Overall explanation
Temperature is the best answer because Microsoft documents it as the setting that controls how random or focused the generated output is. Lower values make results more focused and deterministic, which matches the requirement in the stem.



This item is about identifying the most direct parameter for a specific behavior. While other settings can influence output characteristics, temperature is the clearest and most commonly used control for increasing determinism in chat completions.



Why the other options are incorrect:

• B. top_p is plausible because it also affects token selection and sampling behavior. However, Microsoft recommends adjusting either temperature or top_p, not both, and the stem asks for the most direct choice for more focused output. Temperature is the simpler and more explicit control for that goal in this phrasing.



• C. presence_penalty is plausible because it changes generation behavior. But Microsoft says presence_penalty encourages the model to talk about new topics based on whether terms have already appeared. That is different from directly making the output more deterministic or less random.



• D. frequency_penalty is plausible because it can reduce repetition. However, Microsoft describes it as penalizing repeated tokens based on their frequency in the text so far. That helps with repetition control, not with directly making overall output more focused in the same way that lowering temperature does.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 15
Correct
You want a chat response to stay concise and more focused. Which TWO configuration choices fit this goal? (Select TWO.)

Your selection is correct
A. Lower temperature

B. Increase both temperature and top_p together

Your selection is correct
C. Set max_completion_tokens

D. Increase presence_penalty to force longer answers

E. Replace the deployment name with the base model family name

Overall explanation
Lowering temperature is a good choice because Microsoft documents that lower values make output more focused and deterministic. Setting max_completion_tokens is also appropriate because Microsoft describes it as an upper bound for the number of tokens that can be generated for a completion, including visible output tokens and reasoning tokens.



The question combines two common goals: controlling randomness and limiting response length. A learner who understands configuration parameters should recognize that temperature addresses response variability, while max_completion_tokens addresses output size.



Why the other options are incorrect:

• B. Increasing both temperature and top_p together is plausible because both are sampling controls. However, Microsoft explicitly recommends altering one or the other, not both, because the interaction is harder to predict. It also moves the configuration away from the goal of a more focused result.



• D. Increasing presence_penalty is plausible because it changes output behavior. But Microsoft defines it as encouraging the model to move toward new topics based on what already appeared in the text. That does not directly make an answer shorter, and it can even push output in a broader direction rather than a tighter one.



• E. Replacing the deployment name with the base model family name is plausible because many learners confuse model identity with deployment identity. However, Azure OpenAI calls require the deployment name when specifying the model parameter. Changing that value to a base model family name is not a tuning choice and can make the call incorrect.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 16
Correct
For each of the following statements, determine whether the statement is correct.



Statement 1: Standard deployment in Foundry resources supports regional, data zone, and global processing.

Statement 2: Serverless API endpoints can be created as global deployments.

Statement 3: Managed compute requires compute quota and is billed per compute uptime.



Select the correct combination.

A. Yes / Yes / No

Your answer is correct
B. Yes / No / Yes

C. No / Yes / Yes

D. No / No / Yes

Overall explanation
Statement 1 is correct. Microsoft says standard deployment in Foundry resources supports regional, data zone, or global processing, which is one reason it is described as the preferred deployment option with the widest set of capabilities.



Statement 2 is incorrect because Microsoft says serverless API endpoints are regional. Statement 3 is correct because Microsoft says managed compute requires compute quota and is billed per compute uptime. That makes the correct pattern Yes / No / Yes.



Why the other options are incorrect:

• A. Yes / Yes / No is close because it gets Statement 1 right. However, it incorrectly marks Statement 2 as true even though serverless API endpoints are documented as regional rather than global. It also incorrectly marks Statement 3 as false even though managed compute does require quota and uptime-based billing.



• C. No / Yes / Yes is incorrect in two places. It denies Statement 1 even though standard deployment in Foundry resources supports regional, data zone, and global processing. It also marks Statement 2 as true even though serverless API deployments are limited to regional deployment.



• D. No / No / Yes gets Statement 2 and Statement 3 right, which makes it a plausible distractor. But it incorrectly marks Statement 1 as false even though the deployment overview explicitly lists regional, data zone, and global processing for standard deployment in Foundry resources. Because of that miss, the full combination is still wrong.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 17
Correct
You want to cap how much text a chat completion can generate, including visible output and reasoning tokens.



Which parameter is the best fit?

Your answer is correct
A. max_completion_tokens

Explanation
The parameter "max_completion_tokens" is the best fit for capping how much text a chat completion can generate, including visible output and reasoning tokens. By setting a maximum number of completion tokens, you can control the length of the generated text and ensure it does not exceed a certain limit.
B. presence_penalty

Explanation
The parameter "presence_penalty" is not the best fit for capping how much text a chat completion can generate. Presence penalty is used to encourage the model to maintain the presence of certain tokens in the generated text, rather than limiting the overall length of the output.
C. temperature

Explanation
The parameter "temperature" is not the best fit for capping how much text a chat completion can generate. Temperature controls the randomness of the generated text, influencing the diversity and creativity of the output, but it does not directly limit the length of the generated text.
D. top_p

Explanation
The parameter "top_p" is not the best fit for capping how much text a chat completion can generate. Top-p sampling is a method used to control the diversity of the generated text by selecting from the most likely tokens, but it does not specifically limit the overall length of the output.
Overall explanation
max_completion_tokens is correct because Microsoft defines it as an upper bound for the number of tokens that can be generated for a completion, including visible output tokens and reasoning tokens. That is exactly what the question asks about.



This parameter is a direct output-length control. The other options influence style, novelty, or sampling, but they do not act as the primary hard limit on the amount of generated content in the way max_completion_tokens does.



Why the other options are incorrect:

• B. presence_penalty is plausible because it is a request parameter that changes output behavior. However, Microsoft documents it as encouraging the model to talk about new topics based on whether terms already appeared. It does not serve as the main cap on total generated tokens.



• C. temperature is plausible because it strongly affects generation behavior. But Microsoft defines it as a sampling control for randomness and focus, not as a hard output-length limiter. Lowering or raising temperature may change style, yet it does not directly set the maximum completion size.



• D. top_p is plausible because it also changes token selection. However, top_p controls nucleus sampling by limiting the probability mass considered during generation. That is different from bounding the total number of tokens the model can emit.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 18
Correct
You want the model to sample from tokens within a chosen probability mass, such as the top 10 percent. Adjust ________.



Which answer best completes the sentence?

A. frequency_penalty

Explanation
The frequency_penalty parameter is used to adjust the frequency of tokens in the generated text based on their occurrence in the training data. It is not directly related to sampling from tokens within a chosen probability mass like the top 10 percent.
B. temperature

Explanation
The temperature parameter is used to control the randomness of the generated text by scaling the logits before applying softmax. It does not specifically address sampling from tokens within a chosen probability mass like the top 10 percent.
C. max_completion_tokens

Explanation
The max_completion_tokens parameter is used to limit the maximum number of tokens generated by the model in a single call. It does not control the sampling from tokens within a chosen probability mass like the top 10 percent.
Your answer is correct
D. top_p

Explanation
The top_p parameter, also known as nucleus sampling, is used to sample from the most likely tokens within a chosen probability mass. It allows the model to focus on a subset of tokens with the highest probabilities, such as the top 10 percent, improving the quality of generated text.
Overall explanation
top_p is correct because Microsoft defines it as nucleus sampling, where the model considers tokens within the selected probability mass. The documentation even gives the example that 0.1 means the tokens in the top 10% probability mass are considered.



This question checks whether you can distinguish sampling controls from repetition controls and output-length controls. In deployment and configuration topics, small parameter differences matter because the wrong setting can change behavior in a very different way from what you intended.



Why the other options are incorrect:

• A. frequency_penalty is plausible because it changes token behavior during generation. However, Microsoft says it penalizes new tokens based on how frequently they already appeared in the text, which helps reduce repetition. It does not implement probability-mass sampling.



• B. temperature is plausible because it is another widely used sampling-related control. But Microsoft distinguishes temperature from top_p and recommends generally altering one or the other, not both. Temperature changes randomness broadly, whereas top_p specifically constrains the probability mass considered.



• C. max_completion_tokens is plausible because it is another common parameter in the same request body. However, Microsoft defines it as a cap on the number of generated tokens, including visible output and reasoning tokens. It does not determine which probability mass the sampler uses.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 19
Correct
Which THREE pairs are correctly matched? (Select THREE.)

A. delete_analyzer — starts a new analysis request

B. AzureKeyCredential — stores extracted markdown and fields

Your selection is correct
C. begin_analyze — submits content for analysis

D. prebuilt-audio — base analyzer for image extraction

Your selection is correct
E. AnalysisInput(url=file_url) — supplies a file input to the analyzer

Your selection is correct
F. field_schema — defines the structured fields to extract

Overall explanation
begin_analyze is the operation that submits content for analysis, AnalysisInput(url=file_url) is how the examples provide file input, and field_schema is the analyzer-definition component that specifies the structured fields to extract. Those three pairings align directly with the documented SDK and analyzer concepts.



This matching set covers the full lightweight app lifecycle: define what should be extracted, submit content to the analyzer, and then consume the returned results. That is the core implementation pattern for Content Understanding in Foundry-based apps.



Why the other options are incorrect:

• A. delete_analyzer — starts a new analysis request. delete_analyzer is an administrative cleanup method used to remove an analyzer. It does not submit content for analysis, and it is not the documented request path for app runtime extraction. The actual analysis method shown in the SDK examples is begin_analyze.

• B. AzureKeyCredential — stores extracted markdown and fields. AzureKeyCredential is used to authenticate the client when connecting to the service. Extracted markdown and fields appear in the analysis result content, not inside the credential object. This option is plausible because both appear in the same app, but they play completely different roles.

• D. prebuilt-audio — base analyzer for image extraction. The prebuilt base analyzers are modality-specific, and image scenarios derive from prebuilt-image, not prebuilt-audio. The prebuilt analyzers documentation explicitly separates the base analyzers for audio, document, image, and video. This pair fails because it mismatches the content type to the wrong parent analyzer.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 20
Correct
Your Python app is ready to analyze a file URL with an existing analyzer. Which TWO arguments are part of the begin_analyze call? (Select TWO.)

Your selection is correct
A. analyzer_id

B. field_schema

C. AzureKeyCredential

D. base_analyzer_id

Your selection is correct
E. inputs

Overall explanation
The Python examples show begin_analyze being called with an analyzer_id and an inputs collection. The analyzer ID tells the service which analyzer to run, and the inputs collection supplies the file or files to analyze.



This is a core implementation pattern for a lightweight app. The credential is used when creating the client, while schema and base analyzer settings are associated with analyzer definition and customization rather than with the per-request call that submits content for analysis.



Why the other options are incorrect:

• B. field_schema. field_schema is relevant when defining or customizing an analyzer, because it specifies the structured fields you want extracted. It is not one of the arguments shown in the Python begin_analyze request used to submit content for processing. This option is plausible because schema is central to extraction, but it belongs to analyzer definition rather than the runtime submission call.

• C. AzureKeyCredential. AzureKeyCredential is used when creating the ContentUnderstandingClient, not as a parameter passed to begin_analyze. By the time the app submits content for analysis, the client is already authenticated. This distractor tests whether you can separate connection setup from the actual analysis request.

• D. base_analyzer_id. base_analyzer_id is used when creating a custom analyzer so that it inherits from a parent analyzer such as prebuilt-document, prebuilt-image, prebuilt-audio, or prebuilt-video. It is not part of the analysis request for already created analyzers. This makes it a setup-and-definition concept, not a per-call runtime argument.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 21
Correct
For each of the following statements, determine whether the statement is correct.



Statement 1: OCR can extract text from an image.



Statement 2: Speech to Text can transcribe spoken audio into written text.



Statement 3: Named entity recognition is used to create a condensed summary of a paragraph.



Select the correct combination.

Your answer is correct
A. Yes / Yes / No

B. Yes / No / Yes

C. No / Yes / Yes

D. No / No / Yes

Overall explanation
Statement 1 is correct because OCR is specifically used to identify and extract printed or handwritten text from images and documents. This makes it a visual text-extraction technique rather than a text-analysis technique.



Statement 2 is also correct because Speech to Text converts spoken language into text. Statement 3 is incorrect because named entity recognition identifies entities such as people, places, organizations, dates, and quantities, but it does not summarize text. Summarization and entity extraction are different text-analysis tasks.



Why the other options are incorrect:

• B. Yes / No / Yes. This option incorrectly marks Statement 2 as No even though Speech to Text is the standard capability for transcription. It also incorrectly marks Statement 3 as Yes, confusing entity recognition with summarization. The combination fails on two separate capability distinctions.

• C. No / Yes / Yes. This option incorrectly rejects Statement 1 even though OCR is the right technique for extracting visible text from images. It also incorrectly accepts Statement 3, which again mixes up entity extraction with summary generation. Because two statements are misclassified, this combination cannot be correct.

• D. No / No / Yes. This option incorrectly marks both OCR and Speech to Text as false for their core purposes, even though both statements describe their primary uses accurately. It also repeats the same error of treating named entity recognition as summarization. This makes the entire combination inconsistent with the actual service capabilities.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 22
Correct
Your app has already finished analyzing an image and now wants to read the returned summary field.

Snippet

result = poller.result()
content = result.contents[0]
summary = content.fields.get(________)
Which code should replace the missing section?

Your answer is correct
A. "Summary"

Explanation
The correct code to replace the missing section is "Summary" because the field being accessed is the summary field. By using "Summary" as the key, the code will retrieve the value associated with the summary field in the content object.
B. "AnalyzerId"

Explanation
"AnalyzerId" is not the correct code to replace the missing section because the question specifies that the app wants to read the returned summary field, not the analyzer ID. Using "AnalyzerId" would result in trying to access a field that is not related to the summary information.
C. "Credential"

Explanation
"Credential" is not the correct code to replace the missing section because the question is focused on reading the returned summary field, not credentials. Using "Credential" would result in trying to access a field that is not related to the summary information.
D. "Endpoint"

Explanation
"Endpoint" is not the correct code to replace the missing section because the question is specifically asking to read the returned summary field, not the endpoint. Using "Endpoint" would result in trying to access a field that is not related to the summary information.
Overall explanation
The quickstart examples show accessing returned fields by name from content.fields, including "Summary" for image and audio examples. That makes "Summary" the correct field key in this snippet.



This question checks the final step of a lightweight app flow: after the analyzer runs, the app reads structured output from the result object instead of trying to parse raw content manually. That is exactly the value proposition of Content Understanding for app developers.



Why the other options are incorrect:

• B. "AnalyzerId". The analyzer ID is part of the request that tells the service which analyzer to run. It is not shown as the field name used in the quickstart examples for reading extracted result content from content.fields. This option mixes request metadata with the structured field values returned by the analyzer.

• C. "Credential". Credentials are used to authenticate the client before the request is sent. They are not extracted semantic fields stored in the analyzer result. A lightweight app reads returned fields such as summaries, document values, or segments, not its own authentication settings.

• D. "Endpoint". The endpoint identifies the service resource and is part of client setup, not part of the returned extraction content. The result-reading examples focus on contents, markdown, and named fields like "Summary". This distractor checks whether you can separate connection information from actual extracted output.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 23
Correct
You have a recorded training video that contains spoken narration and text on presentation slides. Which TWO techniques help extract both kinds of information? (Select TWO.)

Your selection is correct
A. OCR

Your selection is correct
B. Speech to Text

C. Text to Speech

D. Sentiment analysis

E. Image generation

Overall explanation
OCR is needed to extract the visible text that appears on the slides inside the video frames. Speech to Text is needed to transcribe the spoken narration from the audio track.



This question checks whether you can separate visual text extraction from audio transcription. A single video can contain both modalities, so AI-901 often expects you to identify more than one extraction technique when the scenario includes spoken content and on-screen text.



Why the other options are incorrect:

• C. Text to Speech. Text to Speech produces spoken audio from written text. That is a generation capability, not an extraction capability. It would be useful if you wanted a system to read text aloud, but it would not help you capture information from the existing video.

• D. Sentiment analysis. Sentiment analysis can help determine the emotional tone of text, such as whether a review is positive or negative. However, it does not extract spoken narration from audio and it does not read text from slide images. It could be used after text is extracted, but not as the primary extraction technique here.

• E. Image generation. Image generation creates new images from prompts or other inputs. It does not inspect existing video frames to read text, and it does not process the audio track for transcription. This makes it unrelated to the extraction requirement in the scenario.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 24
Correct
A company is building an AI support assistant for employees with different vision, hearing, and motor needs. Which design choice best reflects inclusiveness?

A. Encryption at rest

Explanation
Encryption at rest focuses on securing data while it is stored, which is important for data privacy and security but does not directly address inclusiveness for employees with different needs related to vision, hearing, and motor skills.
Your answer is correct
B. Alternative input and output methods

Explanation
Alternative input and output methods, such as voice commands, screen readers, and adaptive keyboards, can provide employees with different needs the ability to interact with the AI support assistant effectively. This design choice promotes inclusiveness by accommodating a diverse range of users.
C. Model limitation disclosure

Explanation
Model limitation disclosure involves transparency about the capabilities and limitations of the AI support assistant, which is important for building trust and managing expectations. While transparency is crucial, it does not directly address inclusiveness for employees with different needs related to vision, hearing, and motor skills.
D. Human approval workflow

Explanation
Human approval workflow involves incorporating human oversight or approval in the decision-making process of the AI support assistant. While human oversight is important for ensuring ethical AI practices, it does not directly address inclusiveness for employees with different needs related to vision, hearing, and motor skills.
Overall explanation
Inclusiveness in AI is about designing systems that empower more people to participate effectively, including people with different abilities and interaction preferences. Supporting multiple ways to interact, such as voice input, text input, captions, and accessible output, directly broadens who can use the solution successfully.



This question is testing the difference between inclusiveness and the other responsible AI principles. Encryption aligns more closely with privacy and security, model limitation disclosure aligns with transparency, and human approval workflow aligns more closely with accountability and governance.



Why the other options are incorrect:

• A. Encryption at rest is important, but it addresses privacy and security rather than inclusiveness. It helps protect sensitive data from unauthorized access, which is a different responsible AI concern. A system can be secure and still exclude users who need accessible interaction methods.

• C. Model limitation disclosure is valuable because users should understand what an AI system can and cannot do. That is primarily a transparency consideration, not the main inclusiveness control in this scenario. Publishing limitations does not, by itself, make the assistant usable for people with different accessibility needs.

• D. Human approval workflow can support oversight and escalation paths, which relates more to accountability and sometimes safety. It does not directly expand access for users with different abilities. An inclusive design change must focus on enabling more people to interact successfully with the system.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 25
Incorrect
You are calling an Azure OpenAI deployment from Python. Complete the code so that it uses the correct Azure deployment identifier.

Snippet

from openai import AzureOpenAI
 
deployment_name = "support-chat-prod"
 
client = AzureOpenAI(
    azure_endpoint=endpoint,
    api_key=api_key,
    api_version="2024-10-21",
)
 
response = client.chat.completions.create(
    model=__________,
    messages=[{"role": "user", "content": "Summarize this case."}]
)
Which code should replace the missing section?

Your answer is incorrect
A. "gpt-4o"

Explanation
The value "gpt-4o" does not correspond to the Azure deployment identifier specified in the code snippet. It is an arbitrary string that does not relate to the deployment_name variable or the Azure OpenAI deployment being called.
Correct answer
B. deployment_name

Explanation
The correct choice is to replace the missing section with the deployment_name variable. This variable holds the name of the Azure OpenAI deployment that needs to be called in the code snippet. Using the deployment_name variable ensures that the correct Azure deployment identifier is used in the API call.
C. endpoint

Explanation
The endpoint variable in the code snippet is used to specify the Azure endpoint for the API call. It is not related to the Azure deployment identifier that needs to be provided in the model parameter of the API call. Therefore, using the endpoint variable as the value for the model parameter would be incorrect.
D. api_key

Explanation
The api_key variable in the code snippet is used to provide the API key for authentication in the API call. It is not related to the Azure deployment identifier that needs to be provided in the model parameter of the API call. Therefore, using the api_key variable as the value for the model parameter would be incorrect.
Overall explanation
deployment_name is correct because Azure OpenAI uses the model parameter to refer to the Azure deployment name you created, not merely the underlying model family name. Microsoft states that Azure OpenAI always needs the deployment name, even when you use the model parameter.



This distinction matters in practice because many examples show deployment names that resemble model names, which can confuse learners. The question is testing whether you understand that the deployed resource identity is what the API call needs in Azure OpenAI scenarios.



Why the other options are incorrect:

• A. "gpt-4o" is plausible because Microsoft examples often use names that look like model names, and some users choose matching deployment names. However, the rule is that Azure OpenAI expects the deployment name, not the base model family name by default. In this snippet, the explicitly provided deployment variable is the correct value to use.



• C. endpoint is plausible because endpoints are required when creating the client. But the endpoint identifies the service location, not which deployed model should handle the request. The model argument must point to the deployment identifier instead.



• D. api_key is plausible because it is another required runtime value. However, the API key is used for authentication and has nothing to do with selecting the model deployment. Mixing authentication data with deployment configuration would make the request invalid.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 26
Correct
Customer support calls are recorded. The team wants written transcripts that can later be searched. Which capability should they use?

A. Text to Speech

Explanation
Text to Speech is the process of converting written text into spoken words. It is not the appropriate capability for converting recorded customer support calls into written transcripts for later searching.
B. Speech Translation

Explanation
Speech Translation is the process of translating spoken words from one language to another. While this capability can be useful for multilingual support, it is not the correct capability for converting recorded customer support calls into written transcripts for later searching.
Your answer is correct
C. Speech to Text

Explanation
Speech to Text is the capability that converts spoken words from recorded customer support calls into written transcripts. This capability allows the team to create written transcripts that can be easily searched and referenced later on.
D. OCR

Explanation
OCR (Optical Character Recognition) is the process of converting images of text into machine-encoded text. It is not the appropriate capability for converting recorded customer support calls into written transcripts as it is designed for text within images, not spoken words.
Overall explanation
Speech to Text is the correct capability for converting spoken audio into written text. When the requirement is searchable transcripts from recorded calls, transcription is the key task.



Speech Translation is close, but it adds a translation step and is chosen when the goal is to convert speech from one language into text or speech in another language. Since the scenario asks only for transcripts, Speech to Text is the more direct and appropriate choice.



Why the other options are incorrect:

• A. Text to Speech. Text to Speech converts written text into synthesized spoken audio. That is useful for voice responses, assistants, and accessibility scenarios. It does not help when the starting point is a recorded phone call and the goal is to extract a transcript.

• B. Speech Translation. Speech Translation is useful when audio must be translated into another language while it is recognized. That is broader than the requirement here and introduces functionality that was not requested. In AI-901, when a plain transcript is needed, Speech to Text is the cleaner answer.

• D. OCR. OCR works on visible text in images and documents, not on spoken audio. It can read words from screenshots or scanned files, but it cannot transcribe voices in a recording. Because the source here is audio, OCR is the wrong modality.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 27
Incorrect
After your app calls client.begin_analyze(...), call the poller’s ________ method to wait for the final analysis output.



Which answer best completes the sentence?

Your answer is incorrect
A. get_analyzer()

Explanation
The method get_analyzer() does not align with the task of waiting for the final analysis output after calling client.begin_analyze(...). This method is more likely to retrieve information about the analyzer itself rather than waiting for the analysis output.
B. delete_analyzer()

Explanation
The method delete_analyzer() is not relevant to the task of waiting for the final analysis output after calling client.begin_analyze(...). This method is typically used to delete or remove an analyzer, not to retrieve the analysis results.
Correct answer
C. result()

Explanation
The method result() is the correct choice to call on the poller after calling client.begin_analyze(...) to wait for the final analysis output. This method is specifically designed to retrieve the final result of the analysis process and is essential for obtaining the output.
D. begin_create_analyzer()

Explanation
The method begin_create_analyzer() is not the appropriate method to call on the poller after initiating the analysis process with client.begin_analyze(...). This method is more related to starting the creation of a new analyzer, not waiting for the analysis output.
Overall explanation
The Python SDK examples call client.begin_analyze(...) and then use poller.result() to obtain the completed AnalysisResult. That makes result() the correct completion step in a lightweight application flow.



This matters because Content Understanding analysis is modeled as a long-running operation. Your app starts the analysis, waits for the operation to finish, and then reads returned content such as markdown, fields, transcripts, or segments from the result object.



Why the other options are incorrect:

• A. get_analyzer(). get_analyzer() retrieves analyzer details, such as its configuration or schema, after it already exists. It does not wait for an analysis job to complete and does not return the result of the submitted content-analysis operation. This makes it useful for inspection, but not for collecting the finished extraction output from a poller.

• B. delete_analyzer(). delete_analyzer() removes an analyzer definition. It is an administrative operation, not the method used to complete or read an in-flight analysis request. In fact, calling it in the middle of normal app processing would delete the reusable analyzer rather than return extracted fields.

• D. begin_create_analyzer(). begin_create_analyzer() is used when you are creating a custom analyzer resource, not when you are waiting for an analysis result from already submitted content. It starts a different long-running operation entirely. A lightweight extraction app typically calls this during setup for custom analyzers, not during the normal read-results step after begin_analyze.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 28
Incorrect
You want to identify organizations, locations, and dates in a sentence by using the Azure AI Language client library for Python.

Snippet

from azure.ai.textanalytics import TextAnalyticsClient
from azure.core.credentials import AzureKeyCredential
 
client = TextAnalyticsClient(
    endpoint=endpoint,
    credential=AzureKeyCredential(key)
)
 
document = "Contoso signed a contract in Sydney on 12 March."
 
response = client.________([document])[0]
Which code should replace the missing section?

A. extract_key_phrases

Explanation
The extract_key_phrases method is used to extract key phrases or important terms from a given text. It does not specifically identify organizations, locations, and dates in a sentence, so it is not the correct choice for this scenario.
Correct answer
B. recognize_entities

Explanation
The recognize_entities method is used to identify and extract entities such as organizations, locations, dates, and more from a given text. In this case, it is the correct choice as it specifically targets the identification of organizations, locations, and dates in the sentence.
Your answer is incorrect
C. analyze_sentiment

Explanation
The analyze_sentiment method is used to analyze the sentiment of a given text, such as determining whether the text expresses positive, negative, or neutral sentiment. It does not focus on identifying organizations, locations, and dates, so it is not the correct choice for this scenario.
D. detect_language

Explanation
The detect_language method is used to detect the language of a given text. While language detection is important for text analytics, it does not directly help in identifying organizations, locations, and dates in a sentence. Therefore, it is not the correct choice for this scenario.
Overall explanation
recognize_entities is the correct method because named entity recognition is used to extract categorized items such as organizations, locations, dates, and quantities from text. In this example, the goal is not just to find important words, but to identify specific entity types inside the sentence.



extract_key_phrases would return main talking points, analyze_sentiment would assess emotional tone, and detect_language would identify the language of the text. Those are all valid text-analysis tasks, but they do not match the requirement to pull out typed entities from the sentence.



Why the other options are incorrect:

• A. extract_key_phrases. Key phrase extraction identifies important concepts or talking points in text. It can return useful keywords, but it does not classify them into entity categories such as organization, location, or date. That distinction matters because this question is specifically about structured entity extraction.

• C. analyze_sentiment. Sentiment analysis determines whether text expresses positive, negative, neutral, or mixed opinion. It is helpful for reviews, feedback, and social commentary, but not for extracting named entities. Even if the sentence contains an organization and a place, sentiment analysis is not the operation that returns them as entities.

• D. detect_language. Language detection tells you which language the text is written in. That can be useful as a preprocessing step in multilingual solutions, but it does not identify the organizations, dates, or places contained in the text. It solves a different classification problem from entity recognition.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 29
Correct
A solution must analyze documents, images, audio, and videos and return structured output in a user-defined schema. Which tool best fits this requirement?

A. Azure AI Vision

Explanation
Azure AI Vision is primarily focused on analyzing images and videos to extract insights, detect objects, and recognize text. While it can analyze images and videos, it may not be the best tool for handling documents, audio, and returning structured output in a user-defined schema.
Your answer is correct
B. Azure Content Understanding

Explanation
Azure Content Understanding is designed to analyze various types of content, including documents, images, audio, and videos. It provides capabilities for extracting insights, categorizing content, and returning structured output in a user-defined schema, making it the best fit for the requirement mentioned in the question.
C. Azure AI Search

Explanation
Azure AI Search is a tool focused on enabling search capabilities within applications and websites. While it can help with searching and retrieving information, it is not specifically designed for analyzing documents, images, audio, and videos to return structured output in a user-defined schema.
D. Text to Speech

Explanation
Text to Speech is a tool that converts text input into spoken audio output. While it can handle audio, it is not designed for analyzing documents, images, and videos to return structured output in a user-defined schema as required in the question.
Overall explanation
Azure Content Understanding is the best fit because it is designed to analyze multiple content types such as documents, images, audio, and video and transform them into structured, organized, and searchable data. The requirement for a user-defined output schema also aligns closely with how Content Understanding analyzers work.



Azure AI Vision is focused on image analysis, Azure AI Search is about indexing and retrieval, and Text to Speech generates audio from text. None of those match the full multimodal extraction requirement as directly as Azure Content Understanding.



Why the other options are incorrect:

• A. Azure AI Vision. Azure AI Vision is strong for image-focused analysis tasks such as OCR, tagging, captioning, and object detection. However, this question explicitly requires support across documents, images, audio, and videos with structured output. Because Vision is not the broad multimodal answer here, it is too narrow for the stated need.

• C. Azure AI Search. Azure AI Search is primarily used to build search experiences over indexed content. It can work with enrichment pipelines and extracted content, but it is not itself the core multimodal extraction tool described in the question. In this scenario, the extraction and structuring step comes first, and Content Understanding is the closer match.

• D. Text to Speech. Text to Speech creates spoken audio from written text. It does not analyze documents, images, audio, or videos to extract structured information from them. This makes it an output-generation feature rather than a content-understanding feature.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 30
Correct
Which TWO pieces of information can Azure AI Video Indexer help extract from recorded video content? (Select TWO.)

Your selection is correct
A. Transcript of spoken words

Your selection is correct
B. OCR text from video frames

C. Text-to-speech voice output

D. Key-value pairs from forms

E. Smart crop thumbnails for printing

Overall explanation
Azure AI Video Indexer can analyze recorded video and generate insights from both the audio and visual streams. That includes transcripts of spoken words and OCR text detected within frames, making it suitable for extracting searchable information from video content.



This is a classic multimodal video question in AI-901. Video is not just images over time; it can include spoken narration, visible text, faces, topics, and other signals. Video Indexer is designed to produce those kinds of insights from recorded media.



Why the other options are incorrect:

• C. Text-to-speech voice output. Text to Speech generates audio from written text. That is not an extraction task from recorded video. While a solution might later read extracted text aloud, that is a separate generation step, not a Video Indexer extraction insight.

• D. Key-value pairs from forms. Key-value pair extraction from forms is associated more closely with document-processing tools such as Azure AI Document Intelligence. Video Indexer focuses on insights from video and audio content rather than structured form-field extraction. A frame in a video might contain a form, but that is not the primary modeled use captured by this option.

• E. Smart crop thumbnails for printing. Smart cropping is related to selecting visually relevant regions of an image, not to extracting semantic information from video content. It does not provide transcript text or OCR-based insights. Because the question asks about information extraction from video, this option misses the core requirement.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 31
Correct
You want an Azure OpenAI image model that is best for realism, instruction following, multimodal context, and improved speed/cost.



Which model is the best fit?

A. Whisper

Explanation
Whisper is not specifically designed for image processing or AI tasks. It is more focused on audio processing and speech recognition, making it less suitable for tasks related to image models and multimodal context.
B. Azure AI Vision

Explanation
Azure AI Vision is a service that focuses on computer vision tasks, such as image recognition and analysis. While it is useful for image-related tasks, it may not be the best fit for tasks that require a combination of text and image processing, as well as improved speed/cost considerations.
Your answer is correct
C. gpt-image-1.5

Explanation
The gpt-image-1.5 model is specifically designed for tasks that involve realism, instruction following, multimodal context, and improved speed/cost. It is optimized for handling both text and image data, making it the best fit for the requirements mentioned in the question.
D. Phi-4-mini

Explanation
Phi-4-mini is not a recognized Azure OpenAI image model. It is important to use models that are officially supported and optimized for Azure AI services to ensure compatibility and performance.
Overall explanation
gpt-image-1.5 is correct because Microsoft’s model comparison table describes it as best for realism, instruction following, multimodal context, and improved speed/cost. Those exact cues appear in the stem, so this is a direct model-selection question.



This item tests whether you can distinguish a generative image model from other AI models and services that may appear in Foundry solutions. The requirement is image creation from prompts with strong realism and better efficiency, which aligns with gpt-image-1.5.



Why the other options are incorrect:

• A. Whisper is plausible because it is a well-known model and often appears in multimodal discussions. However, Whisper is used for speech and transcription scenarios rather than generating new visual outputs from text prompts. The Microsoft image generation guidance identifies gpt-image-1.5 and related GPT image models for this task.



• B. Azure AI Vision is plausible because it works with images and computer vision. But Azure AI Vision analyzes visual content; it is not the Azure OpenAI image-generation model described as best for realism and improved speed/cost in this comparison. The question asks for a generative model, not an analysis service.



• D. Phi-4-mini is plausible because Phi models are Microsoft models available in Foundry. However, Phi is not the image-generation family discussed in the Azure OpenAI image model comparison. The stem specifically matches the capabilities listed for gpt-image-1.5.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 32
Correct
You want to modify one selected region of an existing image in the Foundry images playground instead of creating a completely new image. Use ________.



Which answer best completes the sentence?

A. OCR

Explanation
OCR (Optical Character Recognition) is used to extract text from images or documents, not to modify specific regions of an image in the Foundry images playground.
B. Entity recognition

Explanation
Entity recognition is used to identify and extract entities such as names, dates, and locations from text data, not to modify specific regions of an image in the Foundry images playground.
C. Speech translation

Explanation
Speech translation is used to translate spoken language from one language to another, not to modify specific regions of an image in the Foundry images playground.
Your answer is correct
D. Inpainting

Explanation
Inpainting is the process of reconstructing missing or damaged parts of an image, allowing you to modify specific regions of an existing image without creating a completely new one in the Foundry images playground.
Overall explanation
Inpainting is correct because Microsoft says the images playground supports using inpainting to transform parts of an image. The documentation specifically notes that for gpt-image-1, inpainting with text transformation is available.



The key clue in the stem is that only one selected region should change. That is exactly the kind of partial image editing workflow inpainting is meant to handle, while the other options are analysis or language capabilities rather than image generation or editing features.



Why the other options are incorrect:

• A. OCR is plausible because it is commonly associated with image-based workloads. However, OCR extracts printed or handwritten text from existing content and does not generate new pixels in a selected area. Microsoft points to inpainting, not OCR, for changing part of an image in the images playground.



• B. Entity recognition is plausible because it is a genuine AI capability and sometimes appears in beginner exams as a distractor. But it is a text-analysis feature for identifying people, places, organizations, and similar entities in text. It has no role in modifying a region of an image through generative editing.



• C. Speech translation is plausible because it is another transformation workload. However, it operates on spoken language rather than on visual regions inside an image. The image-playground documentation names inpainting as the relevant feature for partial image transformation.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 33
Correct
You want users to see image progress sooner instead of waiting for the final result.



Which TWO settings support this goal? (Select TWO.)

A. quality="high"

Your selection is correct
B. stream=True

C. background="transparent"

D. output_format="jpeg"

Your selection is correct
E. partial_images=2

Overall explanation
B and E are correct because Microsoft says setting stream to true enables streaming responses, and partial_images controls how many intermediate images are returned before the final result. Microsoft also explains that this provides faster visual feedback and improves perceived latency.



The stem is about progress visibility rather than image style or file type. Streaming plus partial images is the documented mechanism for seeing intermediate output as the image is generated.



Why the other options are incorrect:

• A. quality="high" is plausible because quality is a real generation parameter. However, Microsoft notes that lower quality images can be generated faster, so high quality does not support the goal of faster visible progress. It affects fidelity, not intermediate progress delivery.



• C. background="transparent" is plausible because it is a valid image-generation setting. But background transparency changes the visual characteristics of the image, not the delivery of intermediate preview states. Microsoft documents streaming and partial images as the relevant progress controls.



• D. output_format="jpeg" is plausible because output format can matter in image workflows. However, the documentation does not identify JPEG selection as the feature that provides early visual feedback or partial generation events. The supported progress-related settings are stream and partial_images.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 34
Correct
An AI app supports keyboard input, voice input, captions, and screen-reader-friendly output. The app is emphasizing ________.



Which answer best completes the sentence?

A. Transparency

Explanation
Transparency is related to making the AI app's decisions and actions understandable and explainable to users. While transparency is an important aspect of AI ethics, it is not directly related to supporting multiple input and output modalities for accessibility purposes.
B. Accountability

Explanation
Accountability refers to the responsibility and liability for the actions and decisions made by the AI app. While accountability is crucial for ensuring ethical AI practices, it is not directly related to the emphasis on supporting various input and output modalities for accessibility.
Your answer is correct
C. Inclusiveness

Explanation
Inclusiveness is the correct choice as it highlights the importance of accommodating diverse user needs and preferences by supporting multiple input methods like keyboard and voice, as well as providing accessible output options like captions and screen-reader-friendly content. This emphasis on inclusiveness ensures that the AI app is accessible to a wider range of users.
D. Privacy and security

Explanation
Privacy and security are essential aspects of AI app development, but they are not directly related to the emphasis on supporting diverse input and output modalities for accessibility. While privacy and security measures should be implemented in the app, they do not address the specific accessibility requirements mentioned in the question.
Overall explanation
Inclusiveness focuses on making AI systems usable by a wider range of people. Supporting different input and output modes is a classic inclusiveness design choice because it helps users interact in ways that match their abilities and circumstances.



AI-901 expects you to distinguish responsible AI principles from each other in short practical scenarios. When the scenario emphasizes accessibility, multiple interaction paths, or designing for varied user needs, inclusiveness is the best match.



Why the other options are incorrect:

• A. Transparency is about helping users understand how the system works, what it does well, and where its limits are. That could include explanations, model cards, or usage guidance. None of those is the main focus of this scenario, which is about accessible interaction.

• B. Accountability is about assigning responsibility for AI outcomes and ensuring human oversight and governance. It matters for escalation, ownership, and auditability. However, it does not directly describe providing keyboard, voice, captions, and screen-reader support.

• D. Privacy and security protect data, access, and system integrity. Those controls matter in AI systems, but they do not describe making the experience accessible to more people. A solution can protect data well and still fail to be inclusive.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 35
Correct
For each of the following statements, determine whether the statement is correct.



Statement 1: When you use an image-generation model like gpt-image-1 in Foundry, you are presented with the images playground.

Statement 2: The response_format parameter is supported for GPT-image-1 series models.

Statement 3: To get a transparent background on an image generation request, you set background to transparent and use PNG output.



Select the correct combination.

A. Yes / Yes / No

B. Yes / No / No

C. No / Yes / Yes

Your answer is correct
D. Yes / No / Yes

Overall explanation
Statement 1 is correct because Microsoft Foundry says that when you use an image-generation model like gpt-image-1, Foundry presents the images playground. That is the documented visual experimentation surface for image models.



Statement 2 is incorrect because Microsoft says response_format is supported for DALL-E 3 and not for GPT-image-1 series models. Statement 3 is correct because Microsoft states that transparent backgrounds require background="transparent" together with PNG output. That makes the correct combination Yes / No / Yes.



Why the other options are incorrect:

• A. Yes / Yes / No is close because it gets Statement 1 right. However, it incorrectly marks Statement 2 as true even though Microsoft says response_format is not supported for GPT-image-1 series models. It also marks Statement 3 as false even though the documentation explicitly ties transparency to the transparent background setting plus PNG output.



• B. Yes / No / No gets Statement 1 and Statement 2 right, which makes it a plausible distractor. But it incorrectly marks Statement 3 as false even though Microsoft clearly states that transparent image generation uses background="transparent" with PNG output. Because one statement is wrong, the combination is incorrect.



• C. No / Yes / Yes is incorrect in two places. It denies Statement 1 even though Foundry routes image-generation models to the images playground, and it marks Statement 2 as true even though GPT-image-1 does not support response_format. Since two judgments are wrong, the full combination cannot be correct.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 36
Correct
Which THREE pairs are correctly matched? (Select THREE.)

Your selection is correct
A. Serverless API endpoint — pay-as-you-go billing

B. Managed compute — requires no compute quota

Your selection is correct
C. Standard deployment in Foundry resources — supports global processing

D. top_p — penalizes repeated lines verbatim

Your selection is correct
E. frequency_penalty — reduces repeated tokens in generated text

F. presence_penalty — limits the total generated token count

Overall explanation
A, C, and E are the correct matches. Microsoft describes serverless API endpoints as pay-as-you-go, documents standard deployment in Foundry resources as supporting global processing, and defines frequency_penalty as reducing repeated tokens based on existing frequency in the text so far.



This question mixes deployment knowledge with parameter knowledge, which is common in AI-901-style objective testing. A strong answer requires separating hosting choices from generation controls rather than treating all configuration terms as interchangeable.



Why the other options are incorrect:

• B. Managed compute — requires no compute quota is wrong because Microsoft explicitly states that managed compute requires compute quota in the subscription. That quota requirement is one of the distinguishing characteristics of managed compute. The option is plausible because some learners equate “managed” with “fully handled and quota-free,” but that is not what the documentation says here.



• D. top_p — penalizes repeated lines verbatim is wrong because that behavior is tied to frequency_penalty, not top_p. Microsoft defines top_p as nucleus sampling, where the model considers tokens within the chosen probability mass. So this option swaps a repetition-control parameter with a sampling parameter.



• F. presence_penalty — limits the total generated token count is wrong because token-count limiting is handled by max_completion_tokens. Microsoft defines presence_penalty as influencing whether the model moves toward new topics based on whether terms already appeared. It affects topic novelty, not the hard cap on output length.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 37
Incorrect
You want a Microsoft model in Foundry that can generate photorealistic images from natural language prompts.



Which model should you choose?

Your answer is incorrect
A. Azure AI Content Safety

Explanation
Azure AI Content Safety is not a model specifically designed to generate photorealistic images from natural language prompts. It is focused on content moderation and ensuring safety in AI applications by detecting inappropriate content.
B. Whisper

Explanation
Whisper is not a model specifically designed to generate photorealistic images from natural language prompts. It is a platform for sharing secrets and confessions anonymously, not related to image generation.
Correct answer
C. MAI-Image-2

Explanation
MAI-Image-2 is the correct choice as it is a Microsoft model specifically designed for generating photorealistic images from natural language prompts. This model utilizes advanced AI techniques to understand and interpret natural language descriptions and convert them into visually realistic images.
D. Phi-4-mini

Explanation
Phi-4-mini is not a model specifically designed to generate photorealistic images from natural language prompts. It is not related to image generation tasks but may have other applications in the AI domain.
Overall explanation
MAI-Image-2 is correct because Microsoft’s MAI model documentation says MAI-Image-2 and MAI-Image-2e support text-to-image generation from natural language prompts. The same documentation also states that these models are capable of photorealistic image synthesis with realistic imagery and consistent visual structure.



This question is about identifying the right Microsoft generative model family inside Foundry. The requirement is not moderation, audio processing, or language-only generation; it is photorealistic text-to-image output, which maps to the MAI image models.



Why the other options are incorrect:

• A. Azure AI Content Safety is plausible because safety controls are important in generative AI solutions. However, Content Safety is used to detect or moderate unsafe content rather than create new images from prompts. The MAI image model documentation identifies MAI-Image-2 as the relevant text-to-image choice.



• B. Whisper is plausible because it is a well-known model used in AI solutions. But Whisper is associated with audio and transcription workflows, not photorealistic image generation. The requirement in the stem is visual output from natural language prompts, which points to MAI-Image-2 instead.



• D. Phi-4-mini is plausible because Phi is a Microsoft model family available in Foundry. However, Phi models are not the MAI image-generation models documented for photorealistic text-to-image synthesis. The specific Microsoft image model family for this scenario is MAI-Image-2.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 38
Incorrect
You are starting a lightweight Python client for an existing Foundry agent.

Snippet

from azure.ai.projects import AIProjectClient
from azure.identity import DefaultAzureCredential
 
PROJECT_ENDPOINT = "your_project_endpoint"
 
project = ________
openai = project.get_openai_client()
Which code should replace the missing section?

A. AIProjectClient(model=PROJECT_ENDPOINT, credential=DefaultAzureCredential())

Explanation
Choice A is incorrect because the AIProjectClient class does not have a 'model' parameter. The correct parameter to specify the endpoint is 'endpoint'.
Your answer is incorrect
B. AIProjectClient(agent_name=PROJECT_ENDPOINT, credential=DefaultAzureCredential())

Explanation
Choice B is incorrect because the AIProjectClient class does not have an 'agent_name' parameter. The correct parameter to specify the endpoint is 'endpoint'.
Correct answer
C. AIProjectClient(endpoint=PROJECT_ENDPOINT, credential=DefaultAzureCredential())

Explanation
Choice C is correct because the AIProjectClient class requires the 'endpoint' parameter to specify the project endpoint. The DefaultAzureCredential() is used to authenticate the client with Azure services.
D. AIProjectClient(endpoint=PROJECT_ENDPOINT, api_key=DefaultAzureCredential())

Explanation
Choice D is incorrect because the AIProjectClient class does not have an 'api_key' parameter. The correct parameter to specify the endpoint is 'endpoint'.
Overall explanation
The correct constructor uses the Foundry project endpoint together with DefaultAzureCredential(). The current Microsoft quickstart shows AIProjectClient(endpoint=PROJECT_ENDPOINT, credential=DefaultAzureCredential()) as the standard Python pattern for connecting a lightweight client to a Foundry project.



This matters because the project client is the entry point for the rest of the agent workflow. After creating it, you can obtain the OpenAI client and then use Conversations and Responses APIs for the runtime chat flow.



Why the other options are incorrect:

• A. model is not the parameter used to connect the client to a Foundry project. Model selection belongs to agent definition or response-generation scenarios, not to project-client initialization. This option confuses project connection details with model configuration details.

• B. agent_name is also not the constructor field for creating the project client. An agent name is used when creating or referencing an agent, but the client itself must first connect to the project endpoint. This option mixes the identity of the agent with the location of the project.

• D. The quickstart does not use api_key=DefaultAzureCredential(), and that argument shape is incorrect for this pattern. DefaultAzureCredential() is an identity credential object, not an API key value. The documented lightweight Python flow uses credential=DefaultAzureCredential() for the project client.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 39
Correct
After you create an AIProjectClient, which method returns the client used for Responses and Conversations operations with an agent?

A. project.agents.create_version()

Explanation
The method project.agents.create_version() is used to create a new version of an agent within the AIProjectClient. It is not related to returning the client used for Responses and Conversations operations with an agent.
Your answer is correct
B. project.get_openai_client()

Explanation
The method project.get_openai_client() is the correct choice as it returns the client used for Responses and Conversations operations with an agent. This method is specifically designed to retrieve the client for interacting with the agent in the AIProjectClient.
C. project.connections.get()

Explanation
The method project.connections.get() is used to retrieve information about connections within the AIProjectClient. It is not related to returning the client used for Responses and Conversations operations with an agent.
D. project.deployments.list()

Explanation
The method project.deployments.list() is used to list the deployments associated with the AIProjectClient. It is not related to returning the client used for Responses and Conversations operations with an agent.
Overall explanation
The Azure AI Projects client library states that you use .get_openai_client() to obtain the client used for Responses, Conversations, and related agent operations. Microsoft’s migration guidance for the new agent developer experience also says that conversations and responses in Python use project.get_openai_client().



This distinction is important in a lightweight client application. Agent creation and versioning stay on the project client, but runtime chat operations move through the OpenAI client that you retrieve from that project connection.



Why the other options are incorrect:

• A. project.agents.create_version() is for creating an agent version, not for obtaining the runtime client that sends conversation or response calls. It belongs to provisioning and configuration, not to the chat loop of the client application. This is a plausible distractor because it is agent-related, but it is the wrong stage of the workflow.

• C. project.connections.get() is about project connections, not the Responses and Conversations surface used by the lightweight client. Even if a project uses connected resources, that does not replace the dedicated runtime client for agent chat. The question is asking for the method that directly enables agent interaction.

• D. project.deployments.list() is a management operation for deployments, not a way to send or manage agent conversations. Listing deployments can help you inspect assets, but it does not produce the runtime client used for conversations and responses. It therefore does not satisfy the requirement in the question.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 40
Correct
In the current Python response-call pattern, the object used to identify which Foundry agent should handle the request is ________.



Which answer best completes the sentence?

Your answer is correct
A. agent_reference

Explanation
In the current Python response-call pattern, the object used to identify which Foundry agent should handle the request is the agent_reference. This reference is crucial for directing the request to the appropriate agent for processing, ensuring efficient handling of the request within the Foundry system.
B. conversation

Explanation
The term "conversation" does not accurately describe the object used to identify which Foundry agent should handle the request in the current Python response-call pattern. While conversations may occur during the interaction, they do not serve as the primary identifier for routing requests to specific agents within the system.
C. PromptAgentDefinition

Explanation
The term "PromptAgentDefinition" does not align with the object used to identify which Foundry agent should handle the request in the current Python response-call pattern. PromptAgentDefinition may refer to a specific type of agent definition but is not the object used for routing requests to agents.
D. PROJECT_ENDPOINT

Explanation
The term "PROJECT_ENDPOINT" is not the object used to identify which Foundry agent should handle the request in the current Python response-call pattern. PROJECT_ENDPOINT may refer to a specific endpoint related to project management or execution but is not the identifier for directing requests to the appropriate agent within the Foundry system.
Overall explanation
The quickstart sends the request with extra_body={"agent_reference": {"name": AGENT_NAME, "type": "agent_reference"}}. That object is what tells the runtime which agent should process the input.



This is a common AI-901 distinction: a conversation tracks history, a project endpoint identifies where the project lives, and an agent definition is used during creation or versioning. The runtime request itself uses agent_reference to target the agent.



Why the other options are incorrect:

• B. conversation is important, but it identifies the chat session rather than the agent. It keeps the turns connected so the follow-up has context. By itself, it does not tell Foundry which agent to invoke.

• C. PromptAgentDefinition is used when creating an agent version, including properties such as model and instructions. It is not the runtime field passed on every response request. This distractor is plausible because it is agent-related, but it belongs to setup, not invocation.

• D. PROJECT_ENDPOINT tells the SDK how to connect to the Foundry project. It is not the field that identifies a specific agent during the response call. This option mixes project connection information with per-request routing information.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 41
Correct
You already created project and now need the runtime client for agent chat.

Snippet

project = AIProjectClient(
    endpoint=PROJECT_ENDPOINT,
    credential=DefaultAzureCredential(),
)
 
openai = ________
Which code should replace the missing section?

A. AIProjectClient.get_openai_client()

Explanation
The method `get_openai_client()` is not a valid method of the `AIProjectClient` class. Therefore, this choice is incorrect and cannot be used to get the OpenAI client for the project.
Your answer is correct
B. project.get_openai_client()

Explanation
This choice is correct because it calls the `get_openai_client()` method on the `project` object, which is an instance of the `AIProjectClient` class. This method is likely defined within the `AIProjectClient` class to retrieve the OpenAI client for the project.
C. project.openai()

Explanation
This choice is incorrect because it tries to directly access the `openai` attribute of the `project` object, which may not be a valid way to retrieve the OpenAI client. It is more likely that a method like `get_openai_client()` should be called on the `project` object to get the OpenAI client.
D. AIProjectClient.openai_client()

Explanation
This choice is incorrect because it tries to directly access the `openai_client` attribute of the `AIProjectClient` class without instantiating an object. It is more appropriate to call a method on an instance of the class, like `get_openai_client()`, to retrieve the OpenAI client.
Overall explanation
The documented Python pattern is openai = project.get_openai_client(). Microsoft’s client-library documentation and migration guidance both point to this method for Responses and Conversations operations with agents.



This is a lightweight-client pattern question rather than a syntax-trick question. The important concept is that you first connect to the project, and then you obtain the OpenAI client from that project object for the actual conversation flow.



Why the other options are incorrect:

• A. AIProjectClient.get_openai_client() tries to call the method on the class rather than on the instantiated project object. The docs show the method being called on the project instance after the client has been created. This option therefore has the right name but the wrong usage pattern.

• C. project.openai() is not the documented method shown in Microsoft’s Python examples for this workflow. It sounds plausible because it suggests retrieving an OpenAI surface, but it does not match the published agent quickstart. The question is testing the exact client-hand-off pattern used in the current Foundry SDK.

• D. AIProjectClient.openai_client() is also not the documented method name or call pattern. Like option A, it incorrectly treats the class itself as the object to use and also changes the documented method name. This makes it a close-looking distractor, but still wrong.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 42
Correct
A translation assistant assumes all users can hear audio responses clearly. This design ignores ________ considerations.



Which answer best completes the sentence?

A. Accountability

Explanation
Accountability refers to the responsibility and answerability of the system or individuals for their actions. While important in the context of a translation assistant, it is not directly related to the issue of ignoring users' ability to hear audio responses clearly.
Your answer is correct
B. Inclusiveness

Explanation
Inclusiveness is the correct choice as it highlights the importance of considering the needs and abilities of all users, including those who may have difficulty hearing audio responses. Ignoring inclusiveness can lead to exclusion and accessibility issues.
C. Fairness

Explanation
Fairness relates to the impartial and just treatment of individuals. While it is an important consideration in the design of any system, it is not directly related to the issue of ignoring users' ability to hear audio responses clearly.
D. Transparency

Explanation
Transparency refers to the openness and clarity of the system's operations and decisions. While transparency is crucial for building trust and understanding, it is not directly related to the issue of ignoring users' ability to hear audio responses clearly.
Overall explanation
Assuming all users can hear audio clearly is an inclusiveness problem because it fails to account for different abilities and contexts. A more inclusive design would provide alternative outputs such as captions or text so that more people can use the assistant effectively.



This is also a common AI-901 distinction: fairness is about equitable treatment across groups, while inclusiveness is about ensuring the system can be used by a broad range of people. The scenario focuses on accessibility of interaction, which points to inclusiveness.



Why the other options are incorrect:

• A. Accountability is about who is responsible for decisions, approvals, and governance over the AI system. It does not directly describe whether the output format works for users with different hearing needs. The scenario is about usability and access, not ownership.

• C. Fairness is a plausible distractor because it also deals with people and groups, but it is not the best fit here. The issue is not that the assistant is treating groups differently in its decisions. The issue is that the interaction method itself excludes some users.

• D. Transparency would matter if the question were about explaining how the assistant works or disclosing limitations. Here, the main problem is lack of accessible output alternatives. Explaining the system better would not fix the exclusion by itself.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 43
Correct
Which THREE pairs are correctly matched? (Select THREE.)

A. PromptAgentDefinition — stores follow-up messages from the same chat

Your selection is correct
B. get_openai_client() — returns the client used for Responses and Conversations

C. PROJECT_ENDPOINT — contains the agent instructions text

Your selection is correct
D. conversations.create() — starts a multi-turn conversation

E. project.agents.create_version() — sends a user message to an existing conversation

Your selection is correct
F. AIProjectClient — connects to the Foundry project endpoint

Overall explanation
get_openai_client() returns the runtime client for Responses and Conversations operations, conversations.create() starts the multi-turn session, and AIProjectClient connects to the Foundry project endpoint. Those three pairings align directly with Microsoft’s current lightweight client flow.



This question checks whether you can separate setup, runtime chat state, and agent-definition concepts. AI-901 implementation items often test these close distinctions because the names are similar even though the roles are different.



Why the other options are incorrect:

• A. PromptAgentDefinition is used to define an agent’s model and instructions during creation or versioning. It does not store the running history of a chat conversation. Conversation history belongs to the conversation object, not to the agent-definition object.

• C. PROJECT_ENDPOINT identifies the Foundry project endpoint that the SDK connects to. It is not where the instructions text lives. Agent instructions are part of the PromptAgentDefinition when the agent is created.

• E. project.agents.create_version() creates or versions the agent, but it does not send a runtime chat message into an existing conversation. Sending a message is part of the Responses API flow after the conversation and agent reference are in place. This option therefore swaps setup behavior with runtime behavior.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 44
Correct
For each of the following statements, determine whether the statement is correct.



Statement 1: Agent creation and versioning remain on the project client.



Statement 2: A conversation can persist history across turns.



Statement 3: PromptAgentDefinition is the runtime object that stores chat history after each message.



Select the correct combination.

Your answer is correct
A. Yes / Yes / No

B. Yes / No / Yes

C. No / Yes / Yes

D. No / No / Yes

Overall explanation
Statement 1 is correct. Microsoft’s migration guidance says agent creation and versioning remain on the project client, while conversations and responses move through the OpenAI client. That is a key distinction when building a lightweight application for an agent.



Statement 2 is also correct because Microsoft describes a conversation as the component that persists history across turns. Statement 3 is incorrect because PromptAgentDefinition is used to define the agent’s model and instructions during creation, not to hold live conversation history at runtime.



Why the other options are incorrect:

• B. This option incorrectly marks Statement 2 as No even though Microsoft explicitly describes the conversation as the holder of multi-turn history. It also incorrectly marks Statement 3 as Yes, which confuses agent definition with runtime chat state. Because two statement judgments are wrong, this combination cannot be correct.

• C. This option incorrectly marks Statement 1 as No, but the migration guidance clearly says creation and versioning remain on the project client. It also incorrectly marks Statement 3 as Yes, which again assigns conversation-state behavior to PromptAgentDefinition. Only Statement 2 is correct here, so the whole combination is wrong.

• D. This option marks Statement 1 and Statement 2 as No, but both are supported directly by Microsoft’s documentation. It also marks Statement 3 as Yes, which conflicts with the agent-creation quickstart. All three judgments are therefore inconsistent with the documented model.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 45
Correct
To extract printed or handwritten text from an image of a street sign, use ________.



Which answer best completes the sentence?

Your answer is correct
A. OCR

Explanation
OCR (Optical Character Recognition) is the correct choice for extracting printed or handwritten text from an image. OCR technology is specifically designed to recognize and extract text from images, making it the most suitable option for this task.
B. Entity recognition

Explanation
Entity recognition is not the appropriate choice for extracting text from an image of a street sign. Entity recognition is used to identify and classify entities within text data, such as names, dates, and locations, rather than extracting text from images.
C. Speech translation

Explanation
Speech translation is not the correct choice for extracting text from an image of a street sign. Speech translation involves converting spoken language from one language to another, which is not relevant to extracting text from an image.
D. Image generation

Explanation
Image generation is not the correct choice for extracting text from an image of a street sign. Image generation refers to the process of creating new images based on existing data or patterns, rather than extracting text from images.
Overall explanation
OCR stands for optical character recognition. It is the correct technique when the goal is to extract visible text from an image, such as a street sign, label, poster, or scanned page.



In AI-901, this is a common distinction point: OCR is about reading text that appears visually in an image or document. It is different from text analysis techniques that work on already-available text, and it is also different from speech capabilities that work on audio.



Why the other options are incorrect:

• B. Entity recognition. Entity recognition works on text content and identifies items such as people, places, organizations, dates, and quantities. It assumes text is already available to analyze. It does not perform the visual extraction step needed to read words from a sign image.

• C. Speech translation. Speech translation processes spoken audio and can translate recognized speech into another language. It is not designed to inspect image pixels and extract printed words from them. Because the input here is an image, not speech, this is the wrong modality.

• D. Image generation. Image generation creates new visual content from prompts or other inputs. It does not read and extract existing text from an image. In fact, it is the opposite type of task from information extraction.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 46
Correct
You want a preconfigured code snippet that references your agent so you can continue building the lightweight client in a browser-based IDE. Where should you start in Foundry?

A. Monitor page

Explanation
The Monitor page in Foundry is used to track and analyze the performance and behavior of your AI models and agents. It does not provide preconfigured code snippets or references to agents for building lightweight clients in a browser-based IDE.
B. Quotas page

Explanation
The Quotas page in Foundry is used to manage and set limits on the resources and usage of your AI models and agents. It does not provide preconfigured code snippets or references to agents for building lightweight clients in a browser-based IDE.
C. Connections page

Explanation
The Connections page in Foundry is used to manage and configure the connections between your AI models, agents, and external services. It does not provide preconfigured code snippets or references to agents for building lightweight clients in a browser-based IDE.
Your answer is correct
D. Code tab in the agent playground chat pane

Explanation
The Code tab in the agent playground chat pane in Foundry is where you can find preconfigured code snippets that reference your agent. This is the starting point for continuing to build the lightweight client in a browser-based IDE, as it provides the necessary code references and snippets for development.
Overall explanation
Microsoft’s agent development lifecycle documentation says that from the Code tab in the agent playground’s chat pane, you can take a code snippet that references your agent into a dedicated Visual Studio Code for the Web environment. That makes it the most direct place to move from portal testing into lightweight client development.



The playground documentation also explains that the Code tab supports opening code in VS Code for the Web. This is specifically designed to help developers transition from interactive experimentation to implementation without starting from scratch.



Why the other options are incorrect:

• A. The Monitor page is for observing deployed behavior and production performance, not for generating a starter snippet for client development. Monitoring becomes important later, but it is not the entry point for scaffolding the lightweight client. This option points to an operations activity rather than a build activity.

• B. The Quotas page deals with capacity and usage limits, not with code generation or client scaffolding. It can matter for operational planning, but it does not provide the preconfigured snippet referenced in Microsoft’s agent-development guidance. That makes it irrelevant to the task in the question.

• C. The Connections page is about resource connectivity rather than the handoff from playground to code. Even if a client app depends on connected resources, that page is not where Microsoft says to start when you want the preconfigured agent-referencing snippet. The question is asking for the development-focused portal surface, which is the Code tab.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 47
Correct
You want the client app to call an agent that you created in code by defining its model and instructions first.

Snippet

from azure.ai.projects.models import PromptAgentDefinition
 
agent = project.agents.create_version(
    agent_name=AGENT_NAME,
    definition=________,
)
Which code should replace the missing section?

A. {"model": "gpt-5-mini", "instructions": "You are a helpful assistant"}

Explanation
This choice provides a JSON object with the model and instructions for the agent. However, the code snippet expects an instance of the PromptAgentDefinition class, so providing a JSON object directly will not work in this context.
B. DefaultAzureCredential()

Explanation
This choice is incorrect because it is related to authentication credentials and not the definition of the agent model and instructions. The missing section in the code snippet requires an instance of the PromptAgentDefinition class, not a credential object.
Your answer is correct
C. PromptAgentDefinition(model="gpt-5-mini", instructions="You are a helpful assistant")

Explanation
This choice is correct as it creates an instance of the PromptAgentDefinition class with the specified model ("gpt-5-mini") and instructions ("You are a helpful assistant"). This is the correct format and data structure expected by the code snippet to define the agent model and instructions.
D. openai.conversations.create()

Explanation
This choice is incorrect as it references a function from the openai library, which is not relevant to the code snippet provided. The missing section in the code snippet requires an instance of the PromptAgentDefinition class, not a function call from a different library.
Overall explanation
The prompt-agent quickstart shows the agent being created with PromptAgentDefinition(model="gpt-5-mini", instructions="You are a helpful assistant..."). That definition object is the documented way to specify the agent’s model and instructions when you call project.agents.create_version(...).



This is relevant to a lightweight client application because the client often depends on an already created agent. Understanding how the agent was defined helps you distinguish setup-time objects such as PromptAgentDefinition from runtime objects such as conversations and responses.



Why the other options are incorrect:

• A. A plain dictionary is not the documented Python object used in the quickstart for this call. Microsoft shows a PromptAgentDefinition instance, which provides the expected structure for agent creation. This option looks reasonable because it contains the right ideas, but it does not match the documented SDK pattern.

• B. DefaultAzureCredential() is used for authentication when creating the project client, not for defining an agent’s model and instructions. It belongs to connection setup, not to the definition= argument of create_version. This distractor is plausible because the same sample uses that credential, but in a different place.

• D. openai.conversations.create() starts a conversation for multi-turn chat and is part of the runtime interaction flow. It is not the agent-definition object required by project.agents.create_version(...). This option therefore confuses the chat session layer with the agent creation layer.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 48
Incorrect
You are building a small Python app that will send document URLs to Content Understanding and read back extracted results. Which client should the app create first?

Correct answer
A. ContentUnderstandingClient

Explanation
The app should create a ContentUnderstandingClient first because it is specifically designed to interact with the Content Understanding service in Azure AI. This client will provide the necessary methods and functionalities to send document URLs to the service and read back the extracted results.
B. TextAnalyticsClient

Explanation
The TextAnalyticsClient is not the correct choice for this scenario as it is used for text analysis tasks such as sentiment analysis, key phrase extraction, and language detection. It is not designed to interact with the Content Understanding service for processing document URLs.
Your answer is incorrect
C. ImageAnalysisClient

Explanation
The ImageAnalysisClient is not the correct choice for this scenario as it is used for analyzing images and extracting insights from visual content. It is not designed to interact with the Content Understanding service for processing document URLs.
D. SearchClient

Explanation
The SearchClient is not the correct choice for this scenario as it is used for implementing search capabilities in applications using Azure Cognitive Search. It is not designed to interact with the Content Understanding service for processing document URLs.
Overall explanation
ContentUnderstandingClient is the Python SDK entry point for Azure Content Understanding. The quickstart shows creating this client with the resource endpoint and a credential before the app calls analyzers to process documents, images, audio, or video.



That makes it the correct starting point for a lightweight information-extraction application. The other clients belong to different Azure SDKs and solve different workloads, while Content Understanding is the service specifically designed to turn multimodal content into structured output for downstream apps and workflows.



Why the other options are incorrect:

• B. TextAnalyticsClient. TextAnalyticsClient is used for Azure AI Language scenarios such as sentiment analysis, entity recognition, and key phrase extraction. It does not serve as the Content Understanding client entry point, and it is not the SDK object shown in the Content Understanding quickstart. A lightweight app for Content Understanding needs the dedicated client for that service, not the text analytics client from a different product family.

• C. ImageAnalysisClient. ImageAnalysisClient belongs to Azure AI Vision workflows. It can be useful for image analysis tasks, but it is not the SDK client used to call Content Understanding analyzers across document, image, audio, and video inputs. The official Content Understanding quickstart explicitly centers the app around ContentUnderstandingClient, so this option is a plausible but incorrect service confusion.

• D. SearchClient. SearchClient is associated with Azure AI Search operations, not Content Understanding analysis requests. Search is about indexing and retrieval, whereas this dot point is about building a lightweight app that extracts structured information from content. Even though extracted results might later be sent to search, SearchClient is not the correct first client for the Content Understanding app itself.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 49
Correct
You are setting up the Python SDK client for a lightweight Content Understanding app.

Snippet

from azure.core.credentials import AzureKeyCredential
from azure.ai.contentunderstanding import ________
 
client = ________(
    endpoint=endpoint,
    credential=AzureKeyCredential(key)
)
Which code should replace the missing section?

A. SearchClient

Explanation
SearchClient is not the correct choice for setting up a Python SDK client for a lightweight Content Understanding app. SearchClient is typically used for search-related functionalities and is not related to content understanding tasks.
Your answer is correct
B. ContentUnderstandingClient

Explanation
ContentUnderstandingClient is the correct choice for setting up the Python SDK client for a lightweight Content Understanding app. This client is specifically designed for content understanding tasks and provides the necessary functionalities to interact with the content understanding service.
C. TextAnalyticsClient

Explanation
TextAnalyticsClient is not the correct choice for setting up the Python SDK client for a lightweight Content Understanding app. TextAnalyticsClient is typically used for text analytics tasks, such as sentiment analysis and key phrase extraction, and is not tailored for content understanding tasks.
D. ImageAnalysisClient

Explanation
ImageAnalysisClient is not the correct choice for setting up the Python SDK client for a lightweight Content Understanding app. ImageAnalysisClient is typically used for image analysis tasks, such as object detection and image classification, and is not suitable for content understanding tasks.
Overall explanation
The Python quickstart creates a ContentUnderstandingClient by passing the endpoint and an AzureKeyCredential. That is the correct class name for the SDK object that communicates with the Content Understanding service.



This is a good AI-901-style implementation detail because it tests whether you can recognize the correct SDK object rather than just the product name. In a lightweight app, using the right client class is the first step before calling begin_analyze, creating analyzers, or reading returned fields and markdown.



Why the other options are incorrect:

• A. SearchClient. SearchClient is for Azure AI Search interactions such as indexing and querying documents. It is not the client used in the Content Understanding quickstart, and it would not expose Content Understanding analyzer operations. This option is plausible because search often appears downstream from extraction, but it is still the wrong SDK client for the extraction app itself.

• C. TextAnalyticsClient. TextAnalyticsClient is associated with text analysis capabilities in Azure AI Language. While a solution might combine language analysis with extracted text later, it is not the service client used to call Content Understanding analyzers on documents, images, audio, or video. The quickstart and SDK guidance point specifically to ContentUnderstandingClient.

• D. ImageAnalysisClient. ImageAnalysisClient belongs to Azure AI Vision scenarios rather than the dedicated Content Understanding SDK. A lightweight Content Understanding app can work with images, but the service interface still goes through ContentUnderstandingClient. Choosing the vision client here confuses modality with the actual service being called.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 50
Incorrect
You want to generate images with Azure OpenAI image generation models in Foundry.



Which TWO supported approaches should you use? (Select TWO.)

Your selection is correct
A. Image generation API

B. Azure AI Search indexer

Your selection is incorrect
C. OCR pipeline

Correct selection
D. Responses API

E. Speech SDK recognizer

Overall explanation
A and D are correct because Microsoft says you can do image generation through the image generation API or the responses API. The same Microsoft guidance also says you can experiment with image generation in the Foundry portal, but the two supported API approaches named in the documentation are the image generation API and responses API.



This question checks whether you can separate true image-generation entry points from adjacent AI tooling. In AI-901, many distractors are real services, but the correct answer depends on choosing the components that actually create visual output from prompts.



Why the other options are incorrect:

• B. Azure AI Search indexer is plausible because search is often paired with generative AI applications. However, indexing and retrieval do not create original visual outputs from prompts. Microsoft’s image-generation article specifically identifies the image generation API and responses API as supported generation approaches.



• C. OCR pipeline is plausible because OCR is part of computer vision and works with images. But OCR extracts text from existing images or documents rather than generating new images. The question is about creation, so OCR is outside the supported generation approaches named by Microsoft.



• E. Speech SDK recognizer is plausible because it is a real SDK component in AI solutions. However, speech recognition handles spoken audio input and does not generate visual content from prompts. The Microsoft image-generation guidance does not list speech tooling as a supported image-creation approach.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 51
Correct
Which THREE pairs are correctly matched? (Select THREE.)

A. Azure AI Search — creates original images from text prompts

Your selection is correct
B. Images playground — visual experiments for image-generation models

C. response_format — required for GPT-image-1 responses

Your selection is correct
D. output_format="png" — used for transparent background requests

E. OCR — transforms selected parts of an image through inpainting

Your selection is correct
F. b64_json — base64 image data returned in a successful image-generation response

Overall explanation
B, D, and F are correct. Microsoft says image-generation models are presented in the images playground, that transparent background requests use PNG output together with the transparent background setting, and that successful image-generation responses contain base64 image data in b64_json.



This question mixes model workflow, request configuration, and response handling. That reflects real AI-901-style distinctions: the correct answer depends on knowing whether a term belongs to testing, generation settings, or returned image payloads.



Why the other options are incorrect:

• A. Azure AI Search — creates original images from text prompts is wrong because Azure AI Search is used for retrieval and indexing, not for generating original image content. It often appears in broader generative AI architectures, which makes it a plausible distractor, but it is not the image-creation component in Microsoft’s image-generation guidance.



• C. response_format — required for GPT-image-1 responses is wrong because Microsoft says response_format is not supported for GPT-image-1 series models. The service returns base64-encoded image data instead, which is why b64_json is the important output field. This option confuses a request parameter from other model families with the GPT-image-1 behavior.



• E. OCR — transforms selected parts of an image through inpainting is wrong because OCR extracts text from images and documents. Microsoft identifies inpainting as the feature used to transform selected parts of an image in the images playground. So this option swaps an extraction capability with a generative editing capability.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 52
Correct
Which THREE pairs are correctly matched? (Select THREE.)

Your selection is correct
A. Key phrase extraction — identifies main talking points in text

B. Text to Speech — converts spoken audio into written text

Your selection is correct
C. OCR — extracts printed or handwritten text from images

D. Entity recognition — generates a short summary of a document

Your selection is correct
E. Speech to Text — converts spoken audio into written text

F. Image generation — extracts invoice fields from forms

Overall explanation
Key phrase extraction is used to identify important concepts and main talking points in text. OCR is used to read printed or handwritten text from images and documents, and Speech to Text is used to transcribe spoken audio into written text.



These three techniques each target a different modality or extraction need. AI-901 often tests whether you can distinguish text-analysis tasks from image-reading tasks and audio-transcription tasks without confusing them with content generation or unrelated AI features.



Why the other options are incorrect:

• B. Text to Speech — converts spoken audio into written text. Text to Speech does the reverse operation: it converts written text into spoken audio. That makes it an output-generation capability rather than an information-extraction technique from audio. The phrasing is deliberately close to Speech to Text, which is why this distractor is plausible.

• D. Entity recognition — generates a short summary of a document. Entity recognition identifies and categorizes entities such as people, locations, organizations, dates, and quantities in text. It does not create summaries. If the requirement is to condense a document, summarization is the closer concept, not entity recognition.

• F. Image generation — extracts invoice fields from forms. Image generation creates new images, so it belongs to generative output tasks rather than extraction tasks. Extracting invoice fields from forms is a document-understanding scenario. That kind of workload fits document extraction services, not image generation.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 53
Correct
You are following the official Content Understanding quickstart before running a lightweight Python app. Which one-time resource configuration is specifically required?

A. Azure AI Search index schema

Explanation
Azure AI Search index schema is not specifically required for running a lightweight Python app. It is more related to setting up search functionality within an application rather than configuring resources for a Python app.
B. Cosmos DB container

Explanation
Cosmos DB container is not specifically required for running a lightweight Python app. Cosmos DB is a NoSQL database service, and setting up a container within it would be more relevant for data storage and retrieval operations rather than configuring resources for a Python app.
C. Speech voice profile

Explanation
Speech voice profile is not specifically required for running a lightweight Python app. Speech voice profiles are used for customizing speech recognition and synthesis services, which are not directly related to configuring resources for a Python app.
Your answer is correct
D. Model deployment defaults

Explanation
Model deployment defaults are specifically required for running a lightweight Python app as part of the official Content Understanding quickstart. Configuring model deployment defaults ensures that the app can deploy and run machine learning models effectively, which is essential for AI-related applications.
Overall explanation
The current quickstart and custom-analyzer guidance both call out model deployment defaults as a prerequisite for the resource. The setup guidance explains that the Content Understanding resource must be connected to Foundry model deployments, and the quickstart lists model deployment defaults as configured for the resource before running the samples.



That is a Foundry-heavy implementation detail that fits this domain well. The app code itself is lightweight, but the resource still needs the underlying model configuration in place so analyzers can run correctly.



Why the other options are incorrect:

• A. Azure AI Search index schema. An Azure AI Search index might be useful later if you want to store or search extracted results, but it is not listed as the one-time prerequisite for running the Content Understanding quickstart. The official setup focuses on the Foundry resource, endpoint, key, and model deployment defaults. This option confuses a downstream retrieval layer with the direct prerequisite for the extraction service.

• B. Cosmos DB container. A database container can be helpful if you plan to persist extracted data, but it is not a required setup step in the official Content Understanding quickstart. The quickstart does not make database creation a prerequisite to sending files for analysis. This distractor tests whether you can separate app architecture choices from the service setup that the docs explicitly require.

• C. Speech voice profile. A speech voice profile is related to speech synthesis or voice-focused scenarios, not to the documented setup for Content Understanding. The quickstart is about extracting structured data from documents, images, audio, and video, and it does not require configuring a voice profile. This option mixes in a different Azure AI workload that is outside the actual setup steps here.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 54
Incorrect
You want to read text from an image by using the Azure AI Vision client library for Python.

Snippet

from azure.ai.vision.imageanalysis import ImageAnalysisClient
from azure.ai.vision.imageanalysis.models import VisualFeatures
from azure.core.credentials import AzureKeyCredential
 
client = ImageAnalysisClient(
    endpoint=endpoint,
    credential=AzureKeyCredential(key)
)
 
result = client.analyze(
    image_url=image_url,
    visual_features=[________]
)
Which code should replace the missing section?

Correct answer
A. VisualFeatures.READ

Explanation
VisualFeatures.READ is the correct choice as it specifies that the client should analyze the image for text recognition, which is essential for reading text from an image using the Azure AI Vision client library for Python.
Your answer is incorrect
B. VisualFeatures.CAPTION

Explanation
VisualFeatures.CAPTION is not the correct choice for reading text from an image. This option is used to analyze the image for generating a description or caption of the image content, not for text recognition.
C. VisualFeatures.OBJECTS

Explanation
VisualFeatures.OBJECTS is not the correct choice for reading text from an image. This option is used to analyze the image for detecting and identifying objects within the image, not for text recognition.
D. VisualFeatures.SMART_CROPS

Explanation
VisualFeatures.SMART_CROPS is not the correct choice for reading text from an image. This option is used to analyze the image for identifying and cropping the most important parts of the image, not for text recognition.
Overall explanation
VisualFeatures.READ is the correct choice because READ is the OCR feature used to extract printed or handwritten text from an image. When the goal is information extraction from visible text, this is the feature you enable in Image Analysis.



The other visual features serve different purposes. CAPTION describes image content, OBJECTS detects physical objects, and SMART_CROPS identifies image regions for cropping. None of those are designed to return the text that appears in the image.



Why the other options are incorrect:

• B. VisualFeatures.CAPTION. Captioning generates a natural-language description of what appears in an image. That can be useful for accessibility or summarizing scene content, but it does not return the actual words printed in the image. If the image contains a sign or label, captioning might mention it generally without extracting the exact text.

• C. VisualFeatures.OBJECTS. Object detection focuses on identifying physical items and their locations within an image. It is useful for recognizing things like cars, people, or furniture, but not for reading written characters. Even if text appears near an object, object detection is not the correct extraction feature for that requirement.

• D. VisualFeatures.SMART_CROPS. Smart crops help find the most relevant subregions of an image for thumbnailing or framing. This is a composition-oriented feature, not a text-reading feature. It can assist presentation of images, but it does not extract textual content from them.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 55
Incorrect
A company scans supplier invoices as PDFs and wants the invoice number, vendor name, and total amount returned as structured fields. Which service should it use?

Your answer is incorrect
A. OCR

Explanation
OCR (Optical Character Recognition) is a technology that converts different types of documents, such as scanned paper documents, PDF files, or images captured by a digital camera, into editable and searchable data. While OCR can extract text from scanned documents, it may not provide the structured fields required for extracting specific information like invoice number, vendor name, and total amount.
Correct answer
B. Azure AI Document Intelligence

Explanation
Azure AI Document Intelligence is a service provided by Microsoft Azure that uses machine learning models to extract key information from documents, including structured data like invoice numbers, vendor names, and total amounts. This service is specifically designed for extracting structured fields from documents, making it the most suitable choice for the company's requirement.
C. Speech to Text

Explanation
Speech to Text is a service that converts spoken language into written text. While it can be useful for transcribing audio content, it is not designed for extracting structured data from documents like supplier invoices. It may not be the most appropriate choice for the company's specific requirement.
D. Sentiment analysis

Explanation
Sentiment analysis is a natural language processing technique used to determine the sentiment expressed in a piece of text, such as positive, negative, or neutral. While sentiment analysis can be valuable for understanding customer feedback or opinions, it is not relevant to the task of extracting structured fields like invoice numbers, vendor names, and total amounts from supplier invoices in PDF format.
Overall explanation
Azure AI Document Intelligence is designed for extracting structured information from documents such as invoices, receipts, forms, and other text-heavy files. It goes beyond basic text reading by returning fields, tables, and document structure, which is exactly what is needed when the goal is to capture invoice numbers, totals, and vendor details.



OCR is part of the overall document-processing story, but by itself it focuses on reading text rather than understanding document structure and business fields. For invoice extraction, AI-901 expects you to recognize that structured field extraction from forms and business documents maps more directly to Azure AI Document Intelligence.



Why the other options are incorrect:

• A. OCR. OCR is a technique for extracting printed or handwritten text from images and documents. It is useful when you want raw text, but it does not by itself provide the richer document understanding that invoice processing often needs. In this scenario, the requirement is not just to read characters, but to identify meaningful invoice fields in a structured way.

• C. Speech to Text. Speech to Text is used for spoken audio, not scanned PDFs or invoice images. It converts spoken words into written text from microphones, recordings, or audio streams. Since the input here is a document, not an audio source, this capability does not fit the requirement.

• D. Sentiment analysis. Sentiment analysis is a text-analysis technique used to determine whether written text expresses positive, negative, neutral, or mixed sentiment. It helps with opinion mining, reviews, and feedback interpretation, but it does not extract invoice fields. Even if invoice text were available, sentiment analysis would not return vendor name, total amount, or invoice number.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 56
Correct
You want the client app to support follow-up questions in the same chat.

Snippet

openai = project.get_openai_client()
 
# Start the multi-turn session
conversation = ________
Which code should replace the missing section?

A. openai.responses.create()

Explanation
The openai.responses.create() method is typically used to create a new response or generate a response to a specific prompt. It is not directly related to starting a multi-turn conversation session for follow-up questions in the same chat.
B. openai.chat.completions.create()

Explanation
The openai.chat.completions.create() method is used to generate completions for chat prompts, but it is not specifically designed for starting a multi-turn conversation session. It is more focused on generating responses to individual prompts rather than managing a continuous conversation.
C. project.agents.create_version()

Explanation
The project.agents.create_version() method is related to creating a new version of an agent or model within a project. It is not the correct method for starting a multi-turn conversation session for follow-up questions in the same chat.
Your answer is correct
D. openai.conversations.create()

Explanation
The openai.conversations.create() method is the correct choice for starting a multi-turn conversation session. This method is specifically designed for managing conversations with multiple turns, allowing the client app to support follow-up questions in the same chat.
Overall explanation
The current Foundry quickstart shows conversation = openai.conversations.create() before the first question is sent to the agent. That creates the conversation object used to maintain multi-turn context for follow-up messages.



Microsoft’s runtime-components documentation explains that a conversation persists history across turns, while a response is the output produced when the agent processes input. In a lightweight client, creating the conversation first is the documented way to keep a stateful chat session.



Why the other options are incorrect:

• A. openai.responses.create() generates a response, but it does not create the conversation object itself in the quickstart flow. You use it after the conversation exists, not instead of it. Choosing this option skips the step that establishes multi-turn state.

• B. openai.chat.completions.create() is not the method shown in the current Foundry agent quickstart for this workflow. The documented lightweight pattern uses Conversations plus Responses for agent chat. This distractor sounds familiar because it resembles older chat patterns, but it is not the current agent flow shown in the Microsoft docs.

• C. project.agents.create_version() is an agent-management action, not a runtime chat action. It creates or versions the agent definition, while the question is specifically asking how to start the multi-turn session used by the client app. It therefore targets the wrong layer of the workflow.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 57
Correct
You want a follow-up request to stay in the same chat and be handled by the same Foundry agent. Which TWO values belong in the documented Python openai.responses.create(...) pattern? (Select TWO.)

A. model="gpt-5-mini"

Your selection is correct
B. conversation=conversation.id

C. tool_choice="auto"

D. deployment_id="gpt-5-mini"

Your selection is correct
E. extra_body={"agent_reference": {"name": AGENT_NAME, "type": "agent_reference"}}

Overall explanation
The quickstart shows the response call using both conversation=conversation.id and an extra_body that contains agent_reference. The conversation ID keeps the follow-up in the same chat, and the agent reference identifies which agent should process the request.



Those two values are the core of the lightweight client’s runtime pattern. One preserves state across turns, and the other routes the request to the intended Foundry agent rather than treating it as a generic model call.



Why the other options are incorrect:

• A. model="gpt-5-mini" is used for direct model-response patterns, but that is not the key field the quickstart uses when the request is routed through an agent. In the agent flow, the request points to the agent with agent_reference, and the agent itself already encapsulates model configuration. This option is therefore plausible but not one of the two required values asked for here.

• C. tool_choice="auto" can sound reasonable because agents may use tools, but it is not part of the documented minimal Python call pattern shown in the quickstart for this scenario. The question asks for the specific values that keep the chat in the same conversation and on the same agent. This option does not satisfy either of those two needs.

• D. deployment_id="gpt-5-mini" is not the field shown in the current Python agent-chat pattern. Direct deployment concepts and agent routing are related but not the same. In the quickstart, the lightweight client uses agent_reference instead of passing a deployment identifier on the response call.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 58
Correct
Your lightweight app already created the client and imported AnalysisInput. You now want to submit a document URL for extraction.

Snippet

poller = client.begin_analyze(
    analyzer_id="prebuilt-invoice",
    ________
)
result = poller.result()
Which code should replace the missing section?

A. field_schema=[AnalysisInput(url=file_url)],

Explanation
This choice is incorrect because the field_schema parameter is not used to submit a document URL for extraction. It is used to define the schema of the fields in the document being analyzed, not to specify the input document itself.
B. base_analyzer_id="prebuilt-document",

Explanation
This choice is incorrect because the base_analyzer_id parameter is not used to submit a document URL for extraction. It is used to specify the base analyzer ID for the analysis, not to provide the input document URL.
Your answer is correct
C. inputs=[AnalysisInput(url=file_url)],

Explanation
This choice is correct because the inputs parameter is used to specify the input data for the analysis, and in this case, it includes an AnalysisInput object with the URL of the document to be extracted. This is the correct way to submit a document URL for extraction.
D. content_fields=file_url,

Explanation
This choice is incorrect because the content_fields parameter is not used to submit a document URL for extraction. It is used to specify the fields in the content of the document to be analyzed, not to provide the input document URL.
Overall explanation
The Python quickstart uses inputs=[AnalysisInput(url=...)] when calling begin_analyze. That is the correct way to pass the file input into the analyzer request.



This fits a lightweight app pattern well because the code is compact: create the client, submit the analyzer ID and inputs, wait for the poller, and then read the returned content. In AI-901 terms, this is exactly the sort of small SDK flow you should recognize.



Why the other options are incorrect:

• A. field_schema=[AnalysisInput(url=file_url)],. field_schema is not the runtime argument used to supply input files to begin_analyze. Schema belongs to analyzer definition and customization, where you describe the structure of the data you want extracted. Using it here confuses analyzer setup with analyzer execution.

• B. base_analyzer_id="prebuilt-document",. base_analyzer_id is used when creating a custom analyzer resource, not when submitting content to an existing analyzer for processing. The custom-analyzer tutorial shows it inside analyzer definition code rather than inside begin_analyze. This makes it a very plausible implementation distractor, but it is still the wrong place in the call flow.

• D. content_fields=file_url,. The documented Python call does not use a content_fields parameter to pass files. Instead, the SDK examples consistently wrap file URLs inside AnalysisInput objects and pass them through the inputs collection. This option sounds believable, but it does not match the official client pattern.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 59
Correct
You want your lightweight app to use a custom analyzer for extracting structured details from charts in images. Which TWO choices align with the documented custom-analyzer pattern? (Select TWO.)

A. Set base_analyzer_id="prebuilt-audio"

Your selection is correct
B. Set base_analyzer_id="prebuilt-image"

C. Call delete_analyzer to create the analyzer

Your selection is correct
D. Call begin_create_analyzer

E. Store the schema in AzureKeyCredential

Overall explanation
The custom-analyzer tutorial shows image scenarios using base_analyzer_id="prebuilt-image" and then creating the analyzer with begin_create_analyzer. That is the correct pattern when you want to build a reusable analyzer that your lightweight app can call later for structured extraction.



This is a useful implementation distinction for AI-901. A lightweight app often stays small because the extraction logic is packaged into an analyzer definition, while the app itself just submits files and reads back structured fields.



Why the other options are incorrect:

• A. Set base_analyzer_id="prebuilt-audio". The base analyzer needs to match the modality you are customizing. For an image extraction scenario, the tutorial pattern uses prebuilt-image, not prebuilt-audio. Choosing the audio base would misalign the analyzer with the content type being processed.

• C. Call delete_analyzer to create the analyzer. delete_analyzer removes an analyzer and is used for cleanup, not for creation. The creation flow in the tutorial uses begin_create_analyzer, waits for completion, and can then retrieve the analyzer definition with get_analyzer. This option is a method-name confusion trap, not the actual setup step.

• E. Store the schema in AzureKeyCredential. AzureKeyCredential is for authentication, not for storing field schema or extraction logic. The schema belongs in the analyzer definition, specifically in field_schema, while the credential is used when constructing the client. Mixing them together confuses security setup with extraction configuration.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 60
Correct
You want a compact response from a deployed chat model. Complete the request with the parameter that directly sets an upper bound on generated output.

Snippet

response = client.chat.completions.create(
    model="support-chat-prod",
    messages=[{"role": "user", "content": "Give me a one-sentence summary."}],
    temperature=0.2,
    ________=60
)
Which code should replace the missing section?

A. top_p

Explanation
The 'top_p' parameter is used to control the diversity of the generated responses by sampling from the top p tokens. It does not directly set an upper bound on the generated output, so it is not the correct choice for this scenario.
B. presence_penalty

Explanation
The 'presence_penalty' parameter is used to encourage the model to avoid repeating the same information in the generated response. It does not directly set an upper bound on the generated output, so it is not the correct choice for this scenario.
Your answer is correct
C. max_completion_tokens

Explanation
The 'max_completion_tokens' parameter directly sets an upper bound on the number of tokens in the generated output. By specifying a value for this parameter, you can control the length of the response and ensure it is concise and compact, making it the correct choice for this scenario.
D. frequency_penalty

Explanation
The 'frequency_penalty' parameter is used to penalize the model for generating frequent tokens in the response. It does not directly set an upper bound on the generated output, so it is not the correct choice for this scenario.
Overall explanation
max_completion_tokens is the correct replacement because Microsoft defines it as the parameter that sets an upper bound on generated completion tokens, including visible output tokens and reasoning tokens. That aligns directly with the goal of keeping the response compact.



The rest of the snippet already uses a lower temperature to keep the answer focused. Adding max_completion_tokens complements that by putting an explicit ceiling on how much content the model can generate.



Why the other options are incorrect:

• A. top_p is plausible because it is a common generation parameter and can affect output behavior. However, Microsoft defines it as a nucleus sampling control, not as a token-count limit. It changes which candidate tokens are considered, not how many tokens can ultimately be produced.



• B. presence_penalty is plausible because it changes how the model continues text. But Microsoft says it affects the likelihood of talking about new topics based on what has already appeared. That does not directly set a maximum response length.



• D. frequency_penalty is plausible because it can reduce repetitive output and sometimes makes answers feel tighter. However, Microsoft defines it as a repetition-control parameter based on token frequency, not as a hard output cap. It may influence style, but it is not the right control for the stem.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 61
Incorrect
Refer to:

https://docs.google.com/document/d/11g4CO0Fj8faaxsSCcVNPm7KcB8oWFfQzgkVdnMy1WgY/edit?usp=sharing



You need to satisfy E1, BR1, TR1, and C1.



SignalFrame evaluates the urgency model and determines that it correctly identifies 98% of emergency announcements. Management proposes automatically interrupting broadcasts whenever the model confidence exceeds 95%.



Which control should SignalFrame implement instead?

Correct answer
A. Mandatory editorial approval

Your answer is incorrect
B. Automatic publication above confidence threshold

C. Weekly aggregate accuracy review

D. Anonymous audience feedback collection

Overall explanation
Mandatory editorial approval ensures that an accountable person reviews the recommendation and its supporting information before a broadcast is interrupted. The model can assist with urgency assessment, but it does not make the final public-safety decision. This directly satisfies BR1 and C1 while allowing SignalFrame to benefit from the model’s recommendation.



This control supports both reliability and safety and accountability. Reliability and safety require the system to behave consistently and avoid unacceptable harm, while accountability requires clear human oversight and responsibility. Recording the reviewer and final decision also provides the governance evidence required by TR1.



Why the other options are incorrect:

B. A high confidence score does not guarantee that a recommendation is correct or appropriate in its operational context. Confidence represents the model’s certainty under its learned behaviour rather than a guarantee of real-world safety. Automatic publication would remove the human approval required by BR1. It would also directly violate C1.

C. Weekly accuracy reviews can help identify model drift or recurring classification errors. They occur after recommendations have already been produced and cannot prevent an incorrect interruption at the time of the decision. Aggregate accuracy can also hide rare but high-impact failures. This control should supplement rather than replace editorial approval.

D. Audience feedback can identify confusing or unwanted broadcast decisions after viewers experience them. It does not establish an accountable approval process before the interruption occurs. Anonymous feedback also cannot identify which authorised person accepted the operational risk. It therefore does not satisfy TR1.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 62
Correct
Refer to:

https://docs.google.com/document/d/11g4CO0Fj8faaxsSCcVNPm7KcB8oWFfQzgkVdnMy1WgY/edit?usp=sharing



You need to satisfy E2, BR2, TR2, and C2.



A researcher asks the agent:



“What public warnings has the Australian Government issued about the current cyclone, and when was each warning published?”



The agent must retrieve recently published public information, return a current answer, and include citations to the source pages.



Which tool should you configure?

A. Azure AI Search

B. File Search

Your answer is correct
C. Web search

D. Code Interpreter

Overall explanation
The web search tool allows a Foundry agent to retrieve current information from the public web before generating its response. It can return source-backed answers with inline citations, making it appropriate for recently published government announcements that might not be represented in the model’s training data.



SignalFrame does not need to operate a crawler, periodically ingest websites, or maintain a search index. The agent’s model decides when web retrieval is required based on the user’s request and the tool description. This satisfies BR2, TR2, and C2.



Why the other options are incorrect:

A. Azure AI Search grounds an agent in documents stored in a configured search index. It is appropriate for proprietary or previously indexed content but does not automatically search newly published public webpages. SignalFrame would need to crawl, ingest, and refresh the government content itself. That would violate TR2 and C2.

B. File Search retrieves information from files uploaded or connected to the agent’s vector store. It is suitable for editorial policies, programme notes, or other controlled documents. The current cyclone warnings are external public information that can change after the files are uploaded. File Search would therefore not reliably satisfy BR2.

D. Code Interpreter runs Python for calculations, data analysis, and file generation. It does not independently provide managed retrieval from the current public web. SignalFrame would need to write and maintain scraping or HTTP-request code and handle source citations itself. That would reintroduce the custom search implementation prohibited by C2.

Resources
Domain
Implement AI solutions by using Microsoft Foundry (55–60%)
Question 63
Incorrect
Refer to:

https://docs.google.com/document/d/11g4CO0Fj8faaxsSCcVNPm7KcB8oWFfQzgkVdnMy1WgY/edit?usp=sharing



You need to satisfy E3, BR3, TR3, and C3.



During a live programme, a presenter asks the studio assistant a spoken question. The application must stream microphone audio to the deployed model and begin playing the model’s spoken response before the complete response has been generated.



Which interface should the application use?

Your answer is incorrect
A. Azure Speech batch transcription

Correct answer
B. GPT Realtime API

C. GPT-image-1 deployment

D. Text-embedding deployment

Overall explanation
The GPT Realtime API supports low-latency, speech-in and speech-out interactions with a deployed realtime model. The application can stream audio to the model and receive streaming audio output through WebRTC, WebSocket, or supported real-time communication patterns. This allows the presenter to interact naturally without waiting for a complete recorded file to be processed.



The realtime session manages audio input handling, response generation, and streamed output. Because the deployed model accepts and produces audio natively, SignalFrame does not need to implement separate speech-to-text, text-generation, and text-to-speech stages. This directly satisfies TR3 and C3.



Why the other options are incorrect:

A. Batch transcription converts stored audio files into text asynchronously. It is appropriate for recordings that do not require an immediate interactive response. It does not generate a spoken answer or support a persistent speech-to-speech conversation. The resulting delay would not satisfy BR3.

C. gpt-image-1 generates or edits visual content. It does not accept a live audio conversation and stream spoken responses. Deploying it would provide no speech-recognition or conversational-audio capability. It addresses BR4 rather than BR3.

D. A text-embedding deployment converts text into vectors for semantic similarity and retrieval. It neither processes microphone audio nor generates natural-language or spoken responses. An embedding model could support search for related content after transcription. It cannot implement the live studio assistant.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 64
Correct
Refer to:

https://docs.google.com/document/d/11g4CO0Fj8faaxsSCcVNPm7KcB8oWFfQzgkVdnMy1WgY/edit?usp=sharing



You need to satisfy E4, BR4, TR4, and C4.



A designer provides a programme logo and the following instruction:



“Create a new lower-third graphic using the supplied logo, a transparent background, and space for a presenter’s name.”



The result must be returned as a PNG file.



Which model should SignalFrame deploy?

A. gpt-realtime

B. gpt-4.1

C. text-embedding-3-large

Your answer is correct
D. gpt-image-1

Overall explanation
gpt-image-1 is an image-generation model that can create new visual content from text instructions and can use optional image inputs for editing or reference-based generation. It is therefore appropriate for creating a new lower-third design that incorporates an existing programme logo.



The model supports image-generation parameters including output format and background handling. SignalFrame can request PNG output and a transparent background, producing an asset that can be placed over programme footage. This satisfies TR4 and C4.



Why the other options are incorrect:

A. gpt-realtime supports low-latency audio conversations. It is intended for scenarios involving streaming speech and realtime model interaction. It is not an image-generation deployment. This model is relevant to BR3 rather than BR4.

B. gpt-4.1 is a general-purpose language and reasoning model. Depending on the deployment and interface, it can process supported multimodal inputs, but it is not the specialised image-generation model required to create the final PNG asset. Analysing a logo is different from producing a new production-ready graphic. SignalFrame requires an image-generation deployment.

C. text-embedding-3-large produces numerical vector representations of text. These vectors support semantic search, clustering, and retrieval. The model does not create images or return PNG files. It cannot satisfy any of the output requirements in TR4.

Resources
Domain
Identify AI concepts and capabilities (40–45%)
Question 65
Correct
Refer to:

https://docs.google.com/document/d/11g4CO0Fj8faaxsSCcVNPm7KcB8oWFfQzgkVdnMy1WgY/edit?usp=sharing



You need to satisfy E5, BR5, TR5, and C5.

Each recorded broadcast must be divided according to the individual news stories discussed. For every story, the solution must return:

Start time and end time

Speaker-labelled transcript phrases

Headline

Summary

People mentioned

Locations mentioned

Editorial risk rating

Which component should SignalFrame configure?

Your answer is correct
A. Content Understanding video analyzer

B. Azure AI Video Indexer account

C. Azure Speech batch transcription job

D. Azure Vision Image Analysis request

Overall explanation
A Content Understanding video analyzer can process speech and visual information from a video and return structured, machine-readable output. Its audiovisual elements include transcript phrases with speaker identification and precise timing information. A custom analyzer can also define fields such as headline, summary, people, locations, and editorial risk rating.



Video segmentation can divide the recording into logical sections based on categories or natural-language descriptions supplied in the analyzer configuration. When segmentation is enabled, custom fields can be populated for each resulting story segment rather than only for the complete recording. This directly satisfies BR5 and TR5 while avoiding the separate pipelines prohibited by C5.



Why the other options are incorrect:

B. Azure AI Video Indexer provides media insights such as transcripts, speakers, scenes, shots, topics, and OCR. It is a plausible solution for general broadcast indexing. The requirement also calls for a user-defined metadata schema populated separately for each custom story segment. Content Understanding provides that custom field and segmentation configuration within one analyzer.

C. Azure Speech batch transcription can convert the programme audio into time-aligned text and can support speaker-related transcription scenarios. It does not analyse the visual track or divide the complete broadcast into semantically defined news stories. It also does not populate the required custom metadata schema. Additional segmentation and extraction components would be necessary.

D. Azure Vision Image Analysis processes individual images rather than complete audiovisual recordings. SignalFrame would need to extract frames, separately transcribe audio, correlate timestamps, identify story boundaries, and generate the custom fields. This would create the multi-component processing pipeline prohibited by C5. It therefore cannot satisfy TR5.